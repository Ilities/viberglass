import type { BaseAgentConfig } from "@viberglass/agent-core";

export interface __PascalName__Config extends BaseAgentConfig {
  name: "__NAME__";
  // TODO: add agent-specific config fields here
  // e.g. model?: string;
}
