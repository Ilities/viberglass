// Configuration interface for __DISPLAY_NAME__ integration.
// Add fields matching the configFields defined in plugin.ts.
export interface __PascalName__Config {
  // TODO: add configuration fields
  // Example: apiKey: string
}
