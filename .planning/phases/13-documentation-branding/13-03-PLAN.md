---
phase: 13-documentation-branding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/index.ts
  - platform/backend/src/webhooks/middleware/rawBody.ts
  - viberator/app/src/agents/BaseAgent.ts
  - viberator/app/src/agents/ClaudeCodeAgent.ts
  - viberator/app/src/agents/QwenCodeAgent.ts
autonomous: true

must_haves:
  truths:
    - "Code comments say 'Viberglass' not 'Viberator' for platform references"
    - "Worker class names (ViberatorWorker) remain unchanged"
    - "Worker component references stay as 'Viberator'"
  artifacts:
    - path: "infrastructure/index.ts"
      provides: "Infrastructure entry point with updated comments"
      contains: "Viberglass Infrastructure Stack"
    - path: "viberator/app/src/agents/BaseAgent.ts"
      provides: "Base agent class (unchanged class names)"
      contains: "BaseAgent"
    - path: "viberator/app/src/agents/ClaudeCodeAgent.ts"
      provides: "Claude Code agent implementation"
      contains: "ClaudeCodeAgent"
    - path: "viberator/app/src/agents/QwenCodeAgent.ts"
      provides: "Qwen Code agent implementation"
      contains: "QwenCodeAgent"
  key_links:
    - from: "infrastructure/index.ts"
      to: "codebase"
      via: "Comment terminology aligns with platform branding"
      pattern: "Viberglass.*infrastructure"
---

<objective>
Update inline code comments to reference "Viberglass" as the platform name while preserving "Viberator" for worker/agent class names and component references.

Purpose: Code comments document the system architecture. Platform references in comments should use "Viberglass" while worker components retain "Viberator" naming.

Output: Updated code comments using "Viberglass" for platform, "Viberator" for workers. Class names and component names unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-documentation-branding/13-CONTEXT.md
@.planning/phases/13-documentation-branding/13-RESEARCH.md
@infrastructure/index.ts
@platform/backend/src/webhooks/middleware/rawBody.ts
@viberator/app/src/agents/BaseAgent.ts
@viberator/app/src/agents/ClaudeCodeAgent.ts
@viberator/app/src/agents/QwenCodeAgent.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Update infrastructure/index.ts comments</name>
  <files>infrastructure/index.ts</files>
  <action>
Update infrastructure/index.ts JSDoc comments with the following changes:

1. Update the file header comment block:
   - FROM: "Viberator Infrastructure Stack"
   - TO: "Viberglass Infrastructure Stack"
   - FROM: "This stack creates the AWS infrastructure for running Viberator workers:"
   - TO: "This stack creates the AWS infrastructure for running Viberator workers:"
   (Note: "Viberator workers" stays unchanged because workers are called Viberators)

2. Update inline comments that reference the platform (not the workers):
   - Look for patterns like "Viberator infrastructure" -> "Viberglass infrastructure"
   - Look for patterns like "viberator-uploads" -> "viberglass-uploads" (resource names)
   - Look for patterns like "viberator-backend" -> "viberglass-backend" (resource names)

3. CRITICAL: Do NOT change:
   - Worker component class names (ViberatorWorker, etc.)
   - Comments specifically about workers (e.g., "Viberator workers")
   - Directory paths in comments (viberator/app)
   - Variable/identifier names (Phase 15 handles resource naming)

4. Resource naming note: The research indicates infrastructure resource naming is Phase 15's scope. However, comments describing resources can be updated now for consistency. Focus on:
   - Comment descriptions
   - JSDoc blocks
   - Inline explanatory comments

Use Edit tool with context-specific old_string patterns. Read the file first to identify exact strings to replace.
  </action>
  <verify>grep -i "viberglass.*infrastructure stack" infrastructure/index.ts returns updated comment</verify>
  <done>infrastructure/index.ts comments reference "Viberglass" platform, "Viberator" workers</done>
</task>

<task type="auto">
  <name>Update platform/backend/src/webhooks/middleware/rawBody.ts comments</name>
  <files>platform/backend/src/webhooks/middleware/rawBody.ts</files>
  <action>
Update platform/backend/src/webhooks/middleware/rawBody.ts comments:

1. Read the file to identify any platform references in comments
2. Update comments that reference "the Viberator platform" to "the Viberglass platform"
3. Keep worker-specific references as "Viberator"

Note: This file may have minimal or no changes if it only contains middleware logic without platform references. Verify the actual content before making changes.

Use Edit tool with exact string matching. Read the file first.
  </action>
  <verify>grep -i "viberglass" platform/backend/src/webhooks/middleware/rawBody.ts shows platform references if present</verify>
  <done>rawBody.ts comments updated if platform references exist</done>
</task>

<task type="auto">
  <name>Review and update agent file comments if needed</name>
  <files>viberator/app/src/agents/BaseAgent.ts, viberator/app/src/agents/ClaudeCodeAgent.ts, viberator/app/src/agents/QwenCodeAgent.ts</files>
  <action>
Review and update comments in the agent files:

1. Read each file to identify comments that reference the platform
2. Update platform references from "Viberator" to "Viberglass" where appropriate
3. CRITICAL: Do NOT change:
   - Class names: BaseAgent, ClaudeCodeAgent, QwenCodeAgent, ViberatorWorker
   - References to specific workers (e.g., "a Viberator", "Viberators")
   - Variable names or function names

4. Focus on:
   - File header comments
   - JSDoc blocks describing the platform context
   - Comments about the system architecture

These files are about worker implementation, so they may have few or no platform references. The "Viberator" in class names and worker references should remain unchanged.

Use Edit tool with exact string matching. Read each file first.
  </action>
  <verify>grep -i "viberglass.*platform\|the viberglass" viberator/app/src/agents/*.ts shows updated platform references</verify>
  <done>Agent file comments updated, worker class names unchanged</done>
</task>

</tasks>

<verification>
After completing all tasks, verify the following:

```bash
# 1. Check infrastructure comments for platform references
grep -i "viberglass.*infrastructure" infrastructure/index.ts
# Expected: Comment block mentioning "Viberglass Infrastructure Stack"

# 2. Check that worker references are preserved
grep -i "viberator worker" infrastructure/index.ts
# Expected: Comments still reference "Viberator workers"

# 3. Verify worker class names are unchanged
grep "class.*Viberator" viberator/app/src/workers/*.ts
# Expected: ViberatorWorker class name unchanged

# 4. Check for any missed "the Viberator" platform references
rg "the [Vv]iberator" infrastructure/index.ts platform/backend/src/webhooks/middleware/rawBody.ts viberator/app/src/agents/
# Should return: Only worker references (e.g., "a Viberator"), not platform references

# 5. Verify no Viberator platform references remain in comments
rg "#.*Viberator|//.*Viberator|/\*.*Viberator" --type ts infrastructure/index.ts
# Should return: Only worker-specific references, not platform references
```
</verification>

<success_criteria>
Requirement DOCS-05:
- [ ] Code comments say "Viberglass" not "Viberator" for platform references
- [ ] Worker class names (ViberatorWorker, etc.) remain unchanged
- [ ] Worker component comments still use "Viberator"
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-branding/13-03-SUMMARY.md`
</output>
