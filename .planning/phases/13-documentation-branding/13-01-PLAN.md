---
phase: 13-documentation-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - README.md
  - .planning/MILESTONES.md
autonomous: true

must_haves:
  truths:
    - "PROJECT.md header displays '# Viberglass' not '# Viberator'"
    - "README.md introduces the platform as 'Viberglass' with workers called 'Viberators'"
    - "MILESTONES.md v1.1+ entries use new branding terminology"
    - "v1.0 historical content remains unchanged (historical accuracy)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project documentation header and description"
      contains: "# Viberglass"
    - path: "README.md"
      provides: "Project introduction with correct branding"
      contains: "# Viberglass"
    - path: ".planning/MILESTONES.md"
      provides: "Milestone tracking with updated branding"
      contains: "# Project Milestones: Viberglass"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "README.md"
      via: "Consistent platform naming"
      pattern: "# Viberglass"
---

<objective>
Update project documentation to reflect "Viberglass" as the platform name while keeping "Viberator" for worker/agent components. This is the first visible change in the branding update and establishes the pattern for subsequent changes.

Purpose: Root documentation is the first place users and developers encounter the project. Correct branding here sets expectations for the entire codebase.

Output: Updated PROJECT.md, README.md, and MILESTONES.md with "Viberglass" platform references and "Viberator" worker references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-documentation-branding/13-CONTEXT.md
@.planning/phases/13-documentation-branding/13-RESEARCH.md
@.planning/PROJECT.md
@README.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Update PROJECT.md header and platform references</name>
  <files>.planning/PROJECT.md</files>
  <action>
Update .planning/PROJECT.md with the following changes:

1. Change header from "# Viberator" to "# Viberglass"
2. Update the description to clarify the platform/worker distinction:
   - Change "Users create bug tickets that coding agents automatically fix" to "Users create bug tickets that coding agents (called Viberators) automatically fix"
3. In "Current Milestone: v1.1 Branding Update" section, ensure language reflects:
   - Platform is "Viberglass"
   - Workers are "Viberators"
4. Keep v1.0 MVP section unchanged (historical record)

IMPORTANT: Use Edit tool with exact string matching. Read the file first to get exact context for each replacement.
  </action>
  <verify>grep "^# Viberglass" .planning/PROJECT.md returns the header</verify>
  <done>PROJECT.md header is "# Viberglass", description mentions "Viberators" as workers</done>
</task>

<task type="auto">
  <name>Update README.md introduction and project references</name>
  <files>README.md</files>
  <action>
Update README.md with the following changes:

1. Change header from "# Viberator" to "# Viberglass"
2. Update the description line:
   - FROM: "Agent Orchestrator and Ticket Management Platform. Users create bug tickets that coding agents automatically fix..."
   - TO: "Agent Orchestrator and Ticket Management Platform. Users create bug tickets that coding agents (called Viberators) automatically fix..."
3. In "Quick Start" section, update repository reference:
   - Change "cd viberator" to "cd viberglass" in the clone instruction (this is about the repository name)
4. Update the "Project Structure" section description to clarify the platform/worker distinction

IMPORTANT: DO NOT change directory names in the structure tree (viberator/app, etc.) - those are code paths, not branding.

Use Edit tool with exact string matching. Read the file first.
  </action>
  <verify>grep "^# Viberglass" README.md returns the header</verify>
  <done>README.md header is "# Viberglass", description clarifies Viberators are workers</done>
</task>

<task type="auto">
  <name>Update MILESTONES.md header and v1.1 entries</name>
  <files>.planning/MILESTONES.md</files>
  <action>
Update .planning/MILESTONES.md with the following changes:

1. Change header from "# Project Milestones: Viberator" to "# Project Milestones: Viberglass"
2. Keep v1.0 MVP section COMPLETELY UNCHANGED (it's historical - "Delivered: Complete Viberator platform...")
3. Update the "What's next" line if it mentions v1.1 to use "Viberglass" terminology

CRITICAL: The v1.0 section must stay as-is for historical accuracy. Only update:
- The document header
- Any v1.1 forward-looking statements
- New entries added for v1.1

Use Edit tool with exact string matching. Read the file first.
  </action>
  <verify>grep "^# Project Milestones: Viberglass" .planning/MILESTONES.md returns the header</verify>
  <done>MILESTONES.md header is updated, v1.0 content unchanged</done>
</task>

</tasks>

<verification>
After completing all tasks, verify the following:

```bash
# 1. Check PROJECT.md header
head -1 .planning/PROJECT.md
# Expected: # Viberglass

# 2. Check README.md header
head -1 README.md
# Expected: # Viberglass

# 3. Check MILESTONES.md header
head -1 .planning/MILESTONES.md
# Expected: # Project Milestones: Viberglass

# 4. Verify v1.0 historical content is preserved
grep "Complete Viberator platform" .planning/MILESTONES.md
# Should return: the v1.0 section (historical accuracy)

# 5. Verify worker terminology is preserved
grep -i "viberator" README.md | grep -i "worker\|agent"
# Should show: Viberator mentioned as workers/agents

# 6. Check for any missed "# Viberator" platform references in docs
grep -n "^#.*Viberator" .planning/PROJECT.md README.md .planning/MILESTONES.md
# Should return: only v1.0 historical entries, not current platform references
```
</verification>

<success_criteria>
All success criteria from requirements DOCS-01, DOCS-02, DOCS-06:
- [ ] PROJECT.md header is "# Viberglass"
- [ ] README.md introduces "Viberglass" platform with "Viberators" as workers
- [ ] MILESTONES.md uses "Viberglass" for v1.1+ entries
- [ ] v1.0 historical content preserved unchanged
- [ ] No platform references to "Viberator" remain in current documentation
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-branding/13-01-SUMMARY.md`
</output>
