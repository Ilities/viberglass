---
phase: 13-documentation-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true

must_haves:
  truths:
    - 'package.json "name" field is "viberglass-monorepo"'
    - 'package.json "description" field references "Viberglass platform"'
    - "Workspace paths remain unchanged (viberator/app, etc.)"
  artifacts:
    - path: "package.json"
      provides: "Root package metadata for monorepo"
      contains: '"name": "viberglass-monorepo"'
  key_links:
    - from: "package.json"
      to: "workspace paths"
      via: "workspaces array unchanged"
      pattern: "viberator/app"
---

<objective>
Update package.json metadata to use "viberglass" naming while preserving workspace paths. This updates the package identity without breaking workspace references.

Purpose: Package metadata is used by npm, package registries, and tooling. The name change signals the rebrand while workspace paths must stay intact to avoid breaking the monorepo structure.

Output: Updated package.json with "viberglass-monorepo" name and "Viberglass" description, unchanged workspace paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-documentation-branding/13-CONTEXT.md
@.planning/phases/13-documentation-branding/13-RESEARCH.md
@package.json
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Update package.json name and description fields</name>
  <files>package.json</files>
  <action>
Update package.json with the following changes:

1. Change "name" field:
   - FROM: "name": "viberator-monorepo"
   - TO: "name": "viberglass-monorepo"

2. Change "description" field:
   - FROM: "description": "Viberator monorepo - AI Agent Orchestrator and Platform"
   - TO: "description": "Viberglass monorepo - AI Agent Orchestrator and Platform"

3. CRITICAL: DO NOT modify the "workspaces" array. The workspace paths must stay:
   - "viberator/app"
   - "viberator/infrastructure/infra"
   These are directory paths, not branding references. Changing them would break the monorepo structure.

Use Edit tool with exact string matching for each field. Read the file first to get exact formatting.
  </action>
  <verify>grep -E '"name"|"description"' package.json shows updated values</verify>
  <done>package.json name is "viberglass-monorepo", description references "Viberglass"</done>
</task>

</tasks>

<verification>
After completing the task, verify the following:

```bash
# 1. Check name field
grep '"name"' package.json
# Expected: "name": "viberglass-monorepo"

# 2. Check description field
grep '"description"' package.json
# Expected: "description": "Viberglass monorepo - AI Agent Orchestrator and Platform"

# 3. Verify workspace paths are unchanged
grep -A 10 '"workspaces"' package.json | grep viberator
# Expected: viberator/app, viberator/infrastructure/infra (unchanged)

# 4. Verify package.json is valid JSON
node -e "JSON.parse(require('fs').readFileSync('package.json'))"
# Expected: No error (valid JSON)
```
</verification>

<success_criteria>
Requirements DOCS-03, DOCS-04:
- [ ] package.json name is "viberglass-monorepo"
- [ ] package.json description references "Viberglass platform"
- [ ] Workspace paths remain unchanged (monorepo still works)
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-branding/13-02-SUMMARY.md`
</output>
