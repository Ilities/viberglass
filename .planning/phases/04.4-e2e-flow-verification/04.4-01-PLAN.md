---
phase: 04.4-e2e-flow-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "Docker clanker can be invoked from frontend Run button"
    - "Worker container starts and executes successfully"
    - "Job status updates to completed or failed"
    - "Pull request appears on GitHub (or clear error if not)"
    - "All gaps in implementation are documented"
  artifacts:
    - path: ".planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md"
      provides: "Verification results and gap analysis"
      contains: "## Verification Results"
  key_links:
    - from: "platform/frontend/src/components/run-ticket-modal.tsx"
      to: "/api/tickets/:id/run"
      via: "runTicket API call"
      pattern: "runTicket"
    - from: "platform/backend/src/api/routes/tickets.ts"
      to: "platform/backend/src/workers/WorkerExecutionService.ts"
      via: "workerExecutionService.executeJob"
      pattern: "executeJob"
    - from: "platform/backend/src/workers/invokers/DockerInvoker.ts"
      to: "Docker daemon"
      via: "dockerode API"
      pattern: "container.start"
---

<objective>
Verify the complete end-to-end happy path works for Docker clanker execution and document any gaps found.

Purpose: Confirm that the existing implementation (frontend -> API -> worker -> PR) works as designed before creating infrastructure documentation.
Output: Verification checklist with pass/fail/blocked status and documented gaps for future work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.4-e2e-flow-verification/04.4-RESEARCH.md
@platform/backend/src/workers/invokers/DockerInvoker.ts
@platform/backend/src/api/routes/tickets.ts
@viberator/app/src/workers/viberator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Docker E2E Prerequisites</name>
  <files>.planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md</files>
  <action>
Create verification checklist document and verify prerequisites:

1. Check Docker daemon is available:
   - Run `docker info` to confirm Docker is running
   - Run `docker images | grep viberator` to check if worker image exists

2. Check platform services:
   - Verify backend can start: `cd platform/backend && npm run build`
   - Verify frontend can start: `cd platform/frontend && npm run build`
   - Check for database configuration in platform/backend

3. Check worker image build capability:
   - Look for Dockerfile in viberator/app/
   - Check package.json for docker build scripts

4. Document results in verification file with format:
   ```markdown
   # Phase 04.4 Docker E2E Verification

   ## Prerequisites Check
   | Prerequisite | Status | Notes |
   |--------------|--------|-------|
   | Docker daemon running | PASS/FAIL | ... |
   | Worker image exists | PASS/FAIL | ... |
   | Backend builds | PASS/FAIL | ... |
   | Frontend builds | PASS/FAIL | ... |
   ```

Note: This is a verification task - do NOT fix any issues found, only document them.
  </action>
  <verify>
    - Verification file exists at .planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md
    - All prerequisite checks documented with PASS/FAIL/BLOCKED status
  </verify>
  <done>Prerequisites documented with clear status for each item</done>
</task>

<task type="auto">
  <name>Task 2: Trace Happy Path Code Flow</name>
  <files>.planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md</files>
  <action>
Trace the code path for the Docker happy path and document any gaps:

1. Frontend flow:
   - run-ticket-modal.tsx -> runTicket() -> POST /api/tickets/:id/run
   - Verify clanker selection filters for active + deploymentStrategyId

2. Backend flow:
   - tickets.ts POST /:id/run -> validates ticket, project, clanker
   - Creates job via JobService.submitJob()
   - Invokes worker via WorkerExecutionService.executeJob()
   - Returns 202 Accepted immediately

3. Worker invocation flow:
   - WorkerExecutionService -> WorkerInvokerFactory -> DockerInvoker
   - DockerInvoker creates container with JOB_PAYLOAD env var
   - Container starts (fire and forget)

4. Worker execution flow:
   - viberator.ts reads JOB_PAYLOAD from environment
   - Clones repo, creates branch, executes agent
   - Calls callback to POST /api/jobs/:id/result

5. Result callback flow:
   - jobs.ts POST /:jobId/result updates job status
   - Job status changes to completed or failed

6. Document gaps found:
   - Log streaming: CallbackClient sends `logs: []` - not implemented
   - Job progress: No real-time updates, manual refresh required
   - Any other issues discovered

Append to verification document:
```markdown
## Code Flow Trace

### 1. Frontend -> API
- File: platform/frontend/src/components/run-ticket-modal.tsx
- Flow: [description]
- Gap: [if any]

### 2. API -> Worker
...
```
  </action>
  <verify>
    - Code flow documented for all 5 stages
    - Gaps identified with file locations and future phase references
  </verify>
  <done>Complete code flow traced with gaps documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Prerequisites verification and code flow trace</what-built>
  <how-to-verify>
1. Review .planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md
2. Confirm all prerequisites are documented
3. Confirm code flow trace is complete
4. Verify gaps align with known issues (log streaming, progress updates)

If any prerequisites are blocking (FAIL status), decide whether to:
- Continue with documentation (if issues are environmental)
- Create additional plans to fix blockers
  </how-to-verify>
  <resume-signal>Type "verified" to continue to documentation phase, or describe blockers found</resume-signal>
</task>

</tasks>

<verification>
- .planning/phases/04.4-e2e-flow-verification/04.4-01-VERIFICATION.md exists
- Contains prerequisite checks with status
- Contains code flow trace with 5 stages
- Contains documented gaps for future phases
</verification>

<success_criteria>
- All prerequisites checked and documented
- Code flow from frontend to callback is traced
- Gaps identified and mapped to future phases (Phase 5: polling, Phase 7: streaming)
- Human verification confirms accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-e2e-flow-verification/04.4-01-SUMMARY.md`
</output>
