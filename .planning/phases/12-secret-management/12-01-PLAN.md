---
phase: 12-secret-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/backend/src/config/deployment/SecretProvider.ts
  - platform/backend/src/config/deployment/SsmSecretProvider.ts
  - platform/backend/src/config/deployment/index.ts
autonomous: true

must_haves:
  truths:
    - "SecretProvider interface defines getSecret(environment, key) for deployment-time secrets"
    - "SsmSecretProvider implements SecretProvider using /viberator/{environment}/{category}/{key} path hierarchy"
    - "Provider includes isAvailable() for runtime availability checking"
    - "TypeScript types support both secure and non-secure SSM parameters"
  artifacts:
    - path: "platform/backend/src/config/deployment/SecretProvider.ts"
      provides: "Deployment secret provider interface"
      exports: ["SecretProvider", "SecretCategory"]
    - path: "platform/backend/src/config/deployment/SsmSecretProvider.ts"
      provides: "SSM Parameter Store implementation"
      exports: ["SsmSecretProvider"]
    - path: "platform/backend/src/config/deployment/index.ts"
      provides: "Module exports"
      exports: ["SecretProvider", "SsmSecretProvider", "SecretCategory"]
  key_links:
    - from: "SecretProvider interface"
      to: "Phase 1 CredentialProvider pattern"
      via: "Similar get/put/delete operations pattern"
      pattern: "interface.*Provider.*get.*put.*delete"
---

<objective>
Create deployment secret provider interface with SSM implementation for centralized management of deployment-time secrets across all environments.

Purpose: Establish a provider-based pattern for deployment secrets (distinct from Phase 1's runtime tenant credentials), enabling environment-specific configuration without hardcoding values in workflow files.

Output: SecretProvider interface and SsmSecretProvider implementation with /viberator/{environment}/{category}/{key} path hierarchy.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-secret-management/12-RESEARCH.md
@platform/backend/src/credentials/CredentialProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Create SecretProvider interface and types</name>
  <files>platform/backend/src/config/deployment/SecretProvider.ts</files>
  <action>
    Create SecretProvider interface in platform/backend/src/config/deployment/SecretProvider.ts:

    1. Define SecretProvider interface with methods:
       - getSecret(environment: string, key: string): Promise<string | null>
       - putSecret(environment: string, key: string, value: string, options?: { secure?: boolean }): Promise<void>
       - deleteSecret(environment: string, key: string): Promise<void>
       - isAvailable(): Promise<boolean>

    2. Define SecretCategory enum for path organization:
       - DATABASE = "database"
       - FRONTEND = "frontend"
       - AMPLIFY = "amplify"
       - ECS = "ecs"
       - LAMBDA = "lambda"

    3. Define SecretOptions interface for parameter configuration:
       - secure: boolean (default: true for SecureString, false for plain string)
       - description?: string
       - kmsKeyId?: string (optional KMS key override)

    Use the same pattern as Phase 1's CredentialProvider interface but with environment-first API (getSecret takes environment as parameter, not tenantId).

    Do NOT import from Phase 1 credentials - this is a separate subsystem for deployment secrets, not runtime tenant credentials.
  </action>
  <verify>File exists at platform/backend/src/config/deployment/SecretProvider.ts with interface exported</verify>
  <done>SecretProvider interface defined with getSecret/putSecret/deleteSecret/isAvailable methods</done>
</task>

<task type="auto">
  <name>Implement SsmSecretProvider</name>
  <files>platform/backend/src/config/deployment/SsmSecretProvider.ts</files>
  <action>
    Create SsmSecretProvider class in platform/backend/src/config/deployment/SsmSecretProvider.ts:

    1. Constructor options:
       - region: string (AWS region)
       - kmsKeyId?: string (optional KMS key for SecureString encryption)
       - prefix?: string (default: "/viberator")

    2. Implement getSecret(environment, key):
       - Build SSM path: {prefix}/{environment}/{key}
       - Use aws-sdk client-s3 GetParameterCommand
       - Return null if parameter doesn't exist
       - Handle withDecryption for SecureString parameters

    3. Implement putSecret(environment, key, value, options):
       - Build SSM path from category + key
       - Use PutParameterCommand with Type: SecureString or String
       - Set Overwrite: true for idempotency
       - Apply description if provided

    4. Implement deleteSecret(environment, key):
       - Use DeleteParameterCommand

    5. Implement isAvailable():
       - Try listing parameters with prefix to verify AWS credentials work
       - Return false on auth/region errors

    Use AWS SDK v3 (@aws-sdk/client-ssm) which is already installed in the backend.

    Follow SSM path hierarchy: /viberator/{environment}/{category}/{key}
    Example: /viberator/dev/database/url, /viberator/prod/amplify/appId
  </action>
  <verify>TypeScript compiles without errors: npx tsc --noEmit in platform/backend</verify>
  <done>SsmSecretProvider implements SecretProvider with SSM Parameter Store backend</done>
</task>

<task type="auto">
  <name>Create module index and exports</name>
  <files>platform/backend/src/config/deployment/index.ts</files>
  <action>
    Create platform/backend/src/config/deployment/index.ts with exports:

    - Export SecretProvider interface
    - Export SsmSecretProvider class
    - Export SecretCategory enum
    - Export SecretOptions interface
    - Add JSDoc comments explaining this is for deployment secrets, NOT tenant credentials

    Add file-level comment:
    "Deployment secret management for CI/CD and infrastructure configuration.
     This is distinct from runtime tenant credentials (see ../credentials/).
     Deployment secrets are environment-specific values used during deployment:
     - Database URLs, API endpoints
     - Amplify app IDs, ECR repository names
     - OIDC role ARNs, region configuration

     Runtime tenant credentials (Phase 1) are multi-tenant data:
     - GitHub tokens, Jira API keys
     - SCM provider credentials
     - Per-tenant authentication data"
  </action>
  <verify>Module exports are accessible: import { SecretProvider, SsmSecretProvider, SecretCategory } from './config/deployment'</verify>
  <done>Module exports configured with clear documentation of deployment vs tenant credential distinction</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in platform/backend
2. SecretProvider interface matches specification (getSecret, putSecret, deleteSecret, isAvailable)
3. SsmSecretProvider uses /viberator/{environment}/{category}/{key} path hierarchy
4. Module index exports all public types
5. Code follows Phase 1 CredentialProvider patterns but with environment-first API
</verification>

<success_criteria>
1. SecretProvider interface defines deployment-time secret operations
2. SsmSecretProvider implements SSM Parameter Store backend
3. Path hierarchy follows /viberator/{environment}/{category}/{key} pattern
4. Module exports are properly configured
5. Clear documentation distinguishes deployment secrets from tenant credentials
</success_criteria>

<output>
After completion, create `.planning/phases/12-secret-management/12-01-SUMMARY.md`
</output>
