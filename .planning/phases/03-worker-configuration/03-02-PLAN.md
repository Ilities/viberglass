---
phase: 03-worker-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - viberator/app/package.json
  - viberator/app/src/workers/CredentialProvider.ts
  - viberator/app/src/workers/ConfigLoader.ts
  - viberator/app/src/workers/index.ts
autonomous: true
must_haves:
  truths:
    - "CredentialProvider fetches tenant credentials from SSM using platform AWS credentials"
    - "Worker can fetch credentials without excessive SSM API calls"
    - "CredentialProvider validates required credentials and returns missing list"
    - "ConfigLoader fetches instruction files from S3 using platform AWS credentials"
    - "ConfigLoader logs warning and continues on S3 fetch failures (soft fail)"
    - "ConfigLoader parses s3://bucket/key URL format"
  artifacts:
    - path: "viberator/app/src/workers/CredentialProvider.ts"
      provides: "Worker-side credential fetching from SSM"
      exports: ["CredentialProvider"]
      min_lines: 60
    - path: "viberator/app/src/workers/ConfigLoader.ts"
      provides: "S3 instruction file fetching"
      exports: ["ConfigLoader", "InstructionFile"]
      min_lines: 80
  key_links:
    - from: "CredentialProvider"
      to: "@aws-sdk/client-ssm"
      via: "GetParameterCommand"
      pattern: "GetParameterCommand"
    - from: "ConfigLoader"
      to: "@aws-sdk/client-s3"
      via: "GetObjectCommand"
      pattern: "GetObjectCommand"
    - from: "ConfigLoader"
      to: "AWS credential chain"
      via: "S3Client default credential provider"
      pattern: "new S3Client\\(\\)"
---

<objective>
Create CredentialProvider and ConfigLoader for payload-based worker initialization

Purpose: Enable workers to fetch credentials and instruction files at runtime without calling platform API. CredentialProvider wraps existing getTenantSecret() pattern with batch fetching and validation. ConfigLoader handles S3 instruction file retrieval with graceful degradation.

Output: CredentialProvider class with SSM caching, ConfigLoader class with S3 fetching, @aws-sdk/client-s3 dependency added
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-worker-configuration/03-CONTEXT.md
@.planning/phases/03-worker-configuration/03-RESEARCH.md
@viberator/app/src/utils/secrets.ts
@viberator/app/src/workers/CallbackClient.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @aws-sdk/client-s3 dependency</name>
  <files>viberator/app/package.json</files>
  <action>
    Add @aws-sdk/client-s3 to package.json dependencies:

    cd /home/jussi/Development/viberator/viberator/app
    npm install @aws-sdk/client-s3

    This matches the existing @aws-sdk/client-ssm pattern already in use.
  </action>
  <verify>
  Check package.json contains @aws-sdk/client-s3: grep "@aws-sdk/client-s3" viberator/app/package.json
  </verify>
  <done>
  @aws-sdk/client-s3 installed and available for ConfigLoader S3 operations
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CredentialProvider class</name>
  <files>viberator/app/src/workers/CredentialProvider.ts</files>
  <action>
    Create viberator/app/src/workers/CredentialProvider.ts with:

    1. Constructor taking Logger and optional config:
       - config.region?: string (default: process.env.AWS_REGION || 'us-east-1')
       - config.pathPrefix?: string (default: /viberator/tenants from process.env.TENANT_CONFIG_PATH_PREFIX)

    2. Private SSMClient instance (shared for all requests)

    3. Private cache Map<string, { value: string; expiry: number }> with 5-minute TTL

    4. async getCredential(tenantId: string, key: string): Promise<string | undefined>
       - Build parameter name: ${pathPrefix}/${tenantId}/${key}
       - Check cache first, return if not expired
       - Use GetParameterCommand with WithDecryption: true
       - Cache successful results
       - Return undefined for ParameterNotFound (soft fail)
       - Throw other errors

    5. async getCredentials(tenantId: string, keys: string[]): Promise<Record<string, string | undefined>>
       - Fetch all credentials in parallel using Promise.all
       - Return map of key -> value (undefined for missing)

    6. validateRequired(credentials: Record<string, string | undefined>, required: string[]): { valid: boolean; missing: string[] }
       - Identify missing credentials
       - Log warnings for missing (but don't throw)
       - Return validation result

    Follow existing getTenantSecret() pattern from utils/secrets.ts but add batch fetching and validation methods.
  </action>
  <verify>
  Type check passes: cd /home/jussi/Development/viberator/viberator/app && npx tsc --noEmit
  </verify>
  <done>
  CredentialProvider class with:
  - SSM credential fetching with 5-minute cache
  - Batch credential fetching via getCredentials()
  - Required credential validation with soft fail
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ConfigLoader class for S3 instruction file fetching</name>
  <files>viberator/app/src/workers/ConfigLoader.ts</files>
  <action>
    Create viberator/app/src/workers/ConfigLoader.ts with:

    1. InstructionFile interface:
       export interface InstructionFile {
         fileType: string;
         content: string;
       }

    2. Constructor taking Logger and optional config.region

    3. Private S3Client instance
       - Create with new S3Client({ region }) using DEFAULT credential chain
       - AWS SDK automatically uses: Lambda/EC2 IAM role, environment variables, or ~/.aws/credentials
       - Per CONTEXT.md decision: workers run with IAM role, no explicit credentials needed

    4. private parseS3Url(s3Url: string): { bucket: string; key: string }
       - Parse s3://bucket/key format
       - Use URL constructor for robust parsing
       - Extract bucket from hostname
       - Extract key from pathname (remove leading /)

    5. async fetchInstructionFile(s3Url: string): Promise<string | null>
       - Parse URL to get bucket and key
       - Use GetObjectCommand
       - Transform response.Body to string via transformToString()
       - Return null on error (log warning, soft fail per CONTEXT.md)

    6. async fetchInstructionFiles(files: Array<{ fileType: string; s3Url: string }>): Promise<InstructionFile[]>
       - Fetch all files in parallel via Promise.all
       - Return only successfully fetched files
       - Log warnings for failures but don't fail entire operation

    7. parseConfig(content: string, fileType: string): Record<string, unknown> | null
       - Return { content, type: 'markdown' } for .md files
       - Try JSON.parse for non-markdown files
       - Return null on parse error (log warning)

    Use @aws-sdk/client-s3 GetObjectCommand with transformToString() method.
  </action>
  <verify>
  Type check passes: cd /home/jussi/Development/viberator/viberator/app && npx tsc --noEmit
  </verify>
  <done>
  ConfigLoader class with:
  - S3Client using AWS default credential chain (IAM role in Lambda/EC2)
  - S3 file fetching via GetObjectCommand
  - s3:// URL parsing
  - Soft fail on fetch errors (log warning, return null)
  - Batch fetching with parallel requests
  - Basic config parsing for markdown and JSON
  </done>
</task>

<task type="auto">
  <name>Task 4: Export CredentialProvider and ConfigLoader from workers module</name>
  <files>viberator/app/src/workers/index.ts</files>
  <action>
    Update viberator/app/src/workers/index.ts to export:

    - CredentialProvider (from './CredentialProvider')
    - ConfigLoader (from './ConfigLoader')
    - InstructionFile (from './ConfigLoader')

    Keep existing exports.
  </action>
  <verify>
  Check exports: grep -E "(CredentialProvider|ConfigLoader|InstructionFile)" viberator/app/src/workers/index.ts
  </verify>
  <done>
  CredentialProvider and ConfigLoader exported from workers module
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- CredentialProvider uses @aws-sdk/client-ssm GetParameterCommand
- ConfigLoader uses @aws-sdk/client-s3 GetObjectCommand
- ConfigLoader S3Client uses AWS default credential chain (no explicit credentials)
- Both classes use Logger for consistent logging
- Cache TTL is 5 minutes (300000ms)
- S3 URL parsing handles s3://bucket/key format
</verification>

<success_criteria>
- Workers can fetch credentials from SSM via CredentialProvider
- Workers can fetch instruction files from S3 via ConfigLoader
- Failures log warnings but don't block worker execution
- Credential cache reduces SSM API calls
</success_criteria>

<output>
After completion, create `.planning/phases/03-worker-configuration/03-02-SUMMARY.md`
</output>
