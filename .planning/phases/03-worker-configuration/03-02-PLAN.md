---
phase: 03-worker-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - viberator/app/src/workers/types.ts
  - viberator/app/src/workers/CredentialInjector.ts
  - viberator/app/src/scm/SCMAuthFactory.ts
  - viberator/app/src/scm/types.ts
  - viberator/app/src/scm/providers/GithubAuthProvider.ts
  - viberator/app/src/scm/providers/GitlabAuthProvider.ts
  - viberator/app/src/scm/providers/BitbucketAuthProvider.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - CodingJobData type includes clankerConfig and credentials fields
    - CredentialInjector validates all required credentials exist in environment
    - SCM providers can accept dynamically injected credentials
    - CredentialInjector injects environment variables before agent execution
    - Worker fails fast with clear error if any required credential is missing
  artifacts:
    - path: viberator/app/src/workers/types.ts
      provides: Extended job data types with configuration fields
      contains: "clankerConfig", "credentials"
    - path: viberator/app/src/workers/CredentialInjector.ts
      provides: Credential validation and environment variable injection
      min_lines: 50
      exports: [CredentialInjector]
    - path: viberator/app/src/scm/SCMAuthFactory.ts
      provides: Dynamic credential injection for SCM providers
      contains: "injectCredential", "setProviderToken"
    - path: viberator/app/src/scm/providers/GithubAuthProvider.ts
      provides: GitHub provider with dynamic token support
      contains: "setToken"
    - path: viberator/app/src/scm/types.ts
      provides: Extended SCMAuthProvider interface
      contains: "setToken"
  key_links:
    - from: CredentialInjector
      to: process.env
      via: Environment variable validation before job execution
      pattern: process\.env\[.*\]
    - from: CodingJobData.credentials
      to: CredentialInjector.validate
      via: Array of credential descriptors passed to injector
      pattern: credentials.*forEach.*process\.env
---

<objective>
Extend job types and create CredentialInjector for environment-based credential handling

Purpose: Workers receive credential variable names (not values) in the job payload and read values from their environment. This plan extends the job types to include clanker configuration and credential metadata, creates a CredentialInjector to validate and inject environment variables, and updates SCM providers to accept dynamically injected credentials.

Output: Extended CodingJobData type, CredentialInjector class, and updated SCM auth providers with dynamic credential support
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

@.planning/REQUIREMENTS.md
@.planning/phases/03-worker-configuration/03-RESEARCH.md
@.planning/phases/03-worker-configuration/03-CONTEXT.md
@viberator/app/src/workers/types.ts
@viberator/app/src/scm/SCMAuthFactory.ts
@viberator/app/src/scm/types.ts
@viberator/app/src/scm/providers/GithubAuthProvider.ts
@viberator/app/src/scm/providers/GitlabAuthProvider.ts
@viberator/app/src/scm/providers/BitbucketAuthProvider.ts
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Extend CodingJobData type</name>
  <files>viberator/app/src/workers/types.ts</files>
  <action>
Add new fields to CodingJobData interface to support worker configuration:

1. Add clankerConfig field:
   ```typescript
   export interface ClankerConfig {
     id: string;
     name: string;
     slug: string;
     description?: string | null;
     deploymentStrategyId?: string | null;
     deploymentConfig?: Record<string, unknown> | null;
     environmentVariables?: Record<string, string>;
     configFiles?: ClankerConfigFile[];
   }

   export interface ClankerConfigFile {
     fileType: string;
     content: string;
   }
   ```

2. Add credentials field:
   ```typescript
   export interface CredentialDescriptor {
     name: string;           // Environment variable name (e.g., GITHUB_TOKEN)
     type: string;           // e.g., 'token', 'api_key'
     requiredFor: string[];  // e.g., ['scm', 'api']
   }

   // Add to CodingJobData:
   clankerConfig?: ClankerConfig;
   credentials?: CredentialDescriptor[];
   ```

These fields will be populated by the platform at invocation time. Credentials are variable NAMES only; values come from process.env.
  </action>
  <verify>grep -q "clankerConfig" viberator/app/src/workers/types.ts && grep -q "CredentialDescriptor" viberator/app/src/workers/types.ts && grep -q "ClankerConfig" viberator/app/src/workers/types.ts</verify>
  <done>CodingJobData has clankerConfig and credentials fields with proper type definitions</done>
</task>

<task type="auto">
  <name>Create CredentialInjector class</name>
  <files>viberator/app/src/workers/CredentialInjector.ts</files>
  <action>
Create CredentialInjector class to validate credentials and inject environment variables:

1. Constructor takes (logger: Logger)
2. validateCredentials(credentials: CredentialDescriptor[]): void
   - Checks each credential exists in process.env
   - Throws Error with clear message if any missing: "Missing required credential: {name}"
   - Logs all found credentials (names only, not values) for debugging

3. injectEnvironmentVariables(envVars: Record<string, string>): void
   - Sets each key-value pair on process.env
   - Overwrites existing values if present
   - Logs what was injected (names only, not values per SEC-04)

4. clearInjectedCredentials(): void
   - Clears any environment variables that were injected
   - Important for Lambda container reuse to prevent credential leakage
   - Takes a list of keys to clear

5. getCredentialValue(name: string): string | undefined
   - Helper to read credential from environment
   - Returns process.env[name]

Security note: Never log credential values, only variable names.
  </action>
  <verify>grep -q "class CredentialInjector" viberator/app/src/workers/CredentialInjector.ts && grep -q "validateCredentials" viberator/app/src/workers/CredentialInjector.ts && grep -q "injectEnvironmentVariables" viberator/app/src/workers/CredentialInjector.ts</verify>
  <done>CredentialInjector validates credentials and injects environment variables</done>
</task>

<task type="auto">
  <name>Extend SCMAuthProvider interface for dynamic credentials</name>
  <files>viberator/app/src/scm/types.ts</files>
  <action>
Add setToken method to SCMAuthProvider interface:

```typescript
export interface SCMAuthProvider {
  getName(): string;
  canHandle(repoUrl: string): boolean;
  authenticateUrl(repoUrl: string): string;
  hasCredentials(): boolean;
  getToken(): string | undefined;

  // NEW: Allow dynamic credential injection
  setToken?(token: string): void;
}
```

This allows workers to inject credentials at runtime (read from environment) rather than relying solely on process.env at startup. The setToken method is optional (using ?) so existing providers without it still work.
  </action>
  <verify>grep -q "setToken" viberator/app/src/scm/types.ts</verify>
  <done>SCMAuthProvider interface has optional setToken method</done>
</task>

<task type="auto">
  <name>Add dynamic token support to SCM providers</name>
  <files>
    viberator/app/src/scm/SCMAuthFactory.ts
    viberator/app/src/scm/providers/GithubAuthProvider.ts
    viberator/app/src/scm/providers/GitlabAuthProvider.ts
    viberator/app/src/scm/providers/BitbucketAuthProvider.ts
  </files>
  <action>
Update SCM auth providers to support dynamic credential injection:

1. SCMAuthFactory - Add static methods:
   - injectCredential(providerName: string, token: string): void
     - Finds provider by name and calls setToken if available
   - setProviderToken(repoUrl: string, token: string): void
     - Determines provider from repoUrl and injects token
   - clearDynamicCredentials(): void
     - Clears all injected tokens (call after job for Lambda safety)

2. GithubAuthProvider - Add setToken method:
   - private injectedToken?: string = undefined;
   - setToken(token: string): void { this.injectedToken = token; }
   - Modify getToken() to check injectedToken first, then env vars:
     return this.injectedToken || process.env.GITHUB_TOKEN || process.env.GH_TOKEN;
   - Modify hasCredentials() to check injectedToken too

3. Apply same pattern to GitlabAuthProvider and BitbucketAuthProvider

This allows workers to inject credentials from environment at job start rather than requiring global env vars.
  </action>
  <verify>grep -q "injectCredential" viberator/app/src/scm/SCMAuthFactory.ts && grep -q "setToken" viberator/app/src/scm/providers/GithubAuthProvider.ts</verify>
  <done>All SCM providers support dynamic credential injection via setToken method</done>
</task>

</tasks>

<verification>
1. CodingJobData type compiles with new fields: `npm run build -w @viberator/orchestrator`
2. CredentialInjector throws clear error for missing credentials (test with empty env)
3. SCM providers prioritize injected tokens over environment variables
4. SCMAuthFactory can inject and clear credentials dynamically
5. No credential values are logged (only variable names)
</verification>

<success_criteria>
1. Job payload can specify clanker configuration and credential metadata
2. Workers validate all required credentials before starting job
3. Environment variables from clanker config are injected before agent execution
4. SCM authentication works with dynamically injected credentials
5. Lambda container reuse cannot leak credentials between jobs
</success_criteria>

<output>
After completion, create `.planning/phases/03-worker-configuration/03-02-SUMMARY.md`
</output>
