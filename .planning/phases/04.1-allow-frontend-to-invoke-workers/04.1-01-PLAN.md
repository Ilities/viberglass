---
phase: 04.1-allow-frontend-to-invoke-workers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/backend/migrations/XXX_add_ticket_clanker_to_jobs.ts
  - platform/backend/src/persistence/types/database.ts
  - platform/backend/src/api/routes/tickets.ts
  - platform/backend/src/api/middleware/validation.ts
autonomous: true

must_haves:
  truths:
    - "POST /api/tickets/:ticketId/run creates job and invokes worker"
    - "Response includes jobId and status"
    - "Job record links back to originating ticket and clanker"
    - "Missing required fields (clankerId) returns 400"
    - "Invalid ticket returns 404"
  artifacts:
    - path: "platform/backend/migrations/*_add_ticket_clanker_to_jobs.ts"
      provides: "Database migration for ticket_id and clanker_id columns"
      contains: "ticket_id"
    - path: "platform/backend/src/api/routes/tickets.ts"
      provides: "Run endpoint"
      contains: "router.post('/:id/run'"
  key_links:
    - from: "platform/backend/src/api/routes/tickets.ts"
      to: "WorkerExecutionService"
      via: "executeJob() call"
      pattern: "workerExecutionService\\.executeJob"
    - from: "platform/backend/src/api/routes/tickets.ts"
      to: "JobService"
      via: "submitJob() call"
      pattern: "jobService\\.submitJob"
---

<objective>
Create backend API endpoint for running a ticket as a job with worker invocation.

Purpose: Enable frontend to trigger job execution from a ticket via API call.
Output: POST /api/tickets/:ticketId/run endpoint that creates job, links to ticket/clanker, and invokes worker.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-allow-frontend-to-invoke-workers/04.1-CONTEXT.md
@.planning/phases/04.1-allow-frontend-to-invoke-workers/04.1-RESEARCH.md

# Key existing files
@platform/backend/src/api/routes/tickets.ts
@platform/backend/src/api/routes/jobs.ts
@platform/backend/src/services/JobService.ts
@platform/backend/src/workers/WorkerExecutionService.ts
@platform/backend/src/persistence/types/database.ts
@platform/backend/src/persistence/clanker/ClankerDAO.ts
@platform/backend/src/persistence/project/ProjectDAO.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ticket_id and clanker_id columns to jobs table</name>
  <files>
    platform/backend/migrations/XXX_add_ticket_clanker_to_jobs.ts
    platform/backend/src/persistence/types/database.ts
  </files>
  <action>
    Create a new Kysely migration that adds two nullable columns to the jobs table:
    - ticket_id: UUID, nullable, references tickets(id) ON DELETE SET NULL
    - clanker_id: UUID, nullable, references clankers(id) ON DELETE SET NULL

    Use timestamp-based migration naming: `YYYYMMDDHHMMSS_add_ticket_clanker_to_jobs.ts`

    Update database.ts JobsTable interface to include:
    - ticket_id: string | null
    - clanker_id: string | null

    Run migration to apply changes: `cd platform/backend && npm run db:migrate`
  </action>
  <verify>
    Run: `cd /home/jussi/Development/viberator/platform/backend && npm run db:migrate`
    Verify columns exist: `psql -c "\d jobs"` or check via test query
  </verify>
  <done>
    Jobs table has ticket_id and clanker_id columns with proper foreign key constraints.
    TypeScript types updated to reflect new columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create POST /api/tickets/:id/run endpoint</name>
  <files>
    platform/backend/src/api/routes/tickets.ts
    platform/backend/src/api/middleware/validation.ts
  </files>
  <action>
    Add POST /:id/run endpoint to tickets.ts router:

    1. Add validation middleware in validation.ts:
       - validateRunTicket: Joi schema requiring { clankerId: Joi.string().uuid().required() }

    2. Endpoint logic:
       a. Get ticket by id (return 404 if not found)
       b. Get project by ticket.projectId (return 500 if project missing - data integrity issue)
       c. Get clanker by request.body.clankerId (return 404 if not found)
       d. Validate clanker has deploymentStrategyId and status is 'active' (return 400 if not ready)
       e. Validate project has repositoryUrl (return 400 with "Project has no repository configured")

       f. Create job via JobService.submitJob() with:
          - id: `job_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`
          - tenantId: 'api-server' (hardcoded for now, per RESEARCH.md)
          - repository: project.repositoryUrl
          - task: `${ticket.title}\n\n${ticket.description}`
          - branch: undefined (worker creates branch)
          - baseBranch: 'main'
          - context: { ticketId: ticket.id, stepsToReproduce: ticket.description }
          - settings: { testRequired: false, maxChanges: 10 }
          - timestamp: Date.now()

       g. Store ticket_id and clanker_id on job record (update JobService.submitJob to accept optional fields, or update after creation)

       h. Invoke worker via WorkerExecutionService.executeJob(jobData, clanker)
          - Do NOT await result (fire-and-forget per existing pattern)
          - Instead, use .then().catch() to log errors without blocking response

       i. Return 202 Accepted with:
          { success: true, data: { jobId, status: 'active' } }

    Import WorkerExecutionService and ClankerDAO at top of file.

    IMPORTANT: Per RESEARCH.md pitfall #5, navigate first (return response) before worker invoke completes.
    Use fire-and-forget pattern: invoke worker without awaiting, return response immediately.
  </action>
  <verify>
    Run backend: `cd /home/jussi/Development/viberator/platform/backend && npm run dev`

    Test with curl (assuming ticket and clanker exist):
    ```
    curl -X POST http://localhost:3001/api/tickets/{ticketId}/run \
      -H "Content-Type: application/json" \
      -d '{"clankerId": "{clankerId}"}'
    ```

    Expect: 202 response with { success: true, data: { jobId: "job_...", status: "active" } }

    Test error cases:
    - Missing clankerId: 400
    - Invalid ticket id: 404
    - Invalid clanker id: 404
    - Clanker not active: 400
  </verify>
  <done>
    POST /api/tickets/:id/run endpoint creates job from ticket, stores ticket_id/clanker_id reference,
    invokes worker via WorkerExecutionService, and returns 202 with jobId.
    All error cases return appropriate HTTP status codes.
  </done>
</task>

</tasks>

<verification>
1. Migration applied successfully (no errors)
2. Database has ticket_id and clanker_id columns on jobs table
3. POST /api/tickets/:id/run returns 202 with jobId
4. Job record has ticket_id and clanker_id populated
5. Worker is invoked (check logs for "[WorkerExecutionService] Job invoked successfully")
</verification>

<success_criteria>
- POST /api/tickets/:ticketId/run creates job and invokes worker
- Job record links to ticket and clanker
- Response includes jobId for frontend navigation
- Error handling covers missing ticket, missing clanker, inactive clanker, missing repo
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-allow-frontend-to-invoke-workers/04.1-01-SUMMARY.md`
</output>
