---
phase: 04.2-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/backend/src/__tests__/unit/workers/errors/WorkerError.test.ts
  - platform/backend/src/__tests__/unit/workers/WorkerInvokerFactory.test.ts
  - platform/backend/src/__tests__/unit/workers/invokers/LambdaInvoker.test.ts
autonomous: true

must_haves:
  truths:
    - WorkerError classification getters return correct boolean values
    - WorkerInvokerFactory returns correct invoker type for workerType
    - WorkerInvokerFactory maps clanker strategy to correct invoker
    - LambdaInvoker invokes Lambda function with Event type
    - LambdaInvoker classifies errors as transient or permanent
    - LambdaInvoker throws permanent error when function name missing
  artifacts:
    - path: platform/backend/src/__tests__/unit/workers/errors/WorkerError.test.ts
      provides: Unit tests for WorkerError classification
      min_lines: 40
    - path: platform/backend/src/__tests__/unit/workers/WorkerInvokerFactory.test.ts
      provides: Unit tests for factory registration and lookup
      min_lines: 80
    - path: platform/backend/src/__tests__/unit/workers/invokers/LambdaInvoker.test.ts
      provides: Unit tests for Lambda invocation and error classification
      min_lines: 150
  key_links:
    - from: platform/backend/src/__tests__/unit/workers/invokers/LambdaInvoker.test.ts
      to: "@aws-sdk/client-lambda"
      via: jest.mock()
      pattern: "jest\\.mock\\('@aws-sdk/client-lambda'"
    - from: platform/backend/src/__tests__/unit/workers/WorkerInvokerFactory.test.ts
      to: resetWorkerInvokerFactory
      via: import and call in beforeEach
      pattern: "resetWorkerInvokerFactory"
---

<objective>
Unit tests for WorkerError, WorkerInvokerFactory, and LambdaInvoker following established testing patterns from credential system.

Purpose: Verify error classification logic, factory registration/lookup, and Lambda async invocation behavior with proper error handling.
Output: Three test files with comprehensive coverage of error classification, factory patterns, and Lambda invoker.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.2-testing/04.2-RESEARCH.md
@.planning/phases/04-worker-execution/04-01-SUMMARY.md
@.planning/phases/04-worker-execution/04-02-SUMMARY.md

@platform/backend/src/workers/WorkerInvoker.ts
@platform/backend/src/workers/errors/WorkerError.ts
@platform/backend/src/workers/WorkerInvokerFactory.ts
@platform/backend/src/workers/invokers/LambdaInvoker.ts
@platform/backend/src/__tests__/unit/credentials/AwsSsmProvider.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkerError unit tests</name>
  <files>platform/backend/src/__tests__/unit/workers/errors/WorkerError.test.ts</files>
  <action>
    Create test file at platform/backend/src/__tests__/unit/workers/errors/WorkerError.test.ts

    Test cases:
    1. Constructor creates error with message and classification
    2. isTransient returns true for TRANSIENT classification
    3. isTransient returns false for PERMANENT classification
    4. isPermanent returns true for PERMANENT classification
    5. isPermanent returns false for TRANSIENT classification
    6. name property is 'WorkerError'
    7. cause is stored when provided

    Follow pattern from AwsSsmProvider.test.ts for describe blocks and test structure.
    Use jest.clearAllMocks() in beforeEach even though no mocks (consistency).
  </action>
  <verify>npm test -- WorkerError.test.ts passes all tests</verify>
  <done>All 7 test cases pass, error classification verified</done>
</task>

<task type="auto">
  <name>Task 2: Create WorkerInvokerFactory unit tests</name>
  <files>platform/backend/src/__tests__/unit/workers/WorkerInvokerFactory.test.ts</files>
  <action>
    Create test file at platform/backend/src/__tests__/unit/workers/WorkerInvokerFactory.test.ts

    Mock the invoker classes to avoid AWS SDK initialization in tests:
    ```typescript
    jest.mock('../invokers/LambdaInvoker');
    jest.mock('../invokers/EcsInvoker');
    jest.mock('../invokers/DockerInvoker');
    ```

    Test cases for getInvoker():
    1. Returns LambdaInvoker for 'lambda' workerType
    2. Returns EcsInvoker for 'ecs' workerType
    3. Returns DockerInvoker for 'docker' workerType
    4. Throws error for unregistered workerType

    Test cases for getInvokerForClanker():
    5. Returns LambdaInvoker for clanker with deploymentStrategy.name='lambda'
    6. Returns EcsInvoker for clanker with deploymentStrategy.name='ecs'
    7. Returns DockerInvoker for clanker with deploymentStrategy.name='docker'
    8. Throws error for clanker without deploymentStrategy
    9. Handles case-insensitive strategy names (Lambda, LAMBDA)

    Test cases for registerInvoker():
    10. Allows custom invoker registration
    11. Registered invoker overrides default

    Test cases for getRegisteredTypes():
    12. Returns all registered worker types

    Test cases for singleton:
    13. getWorkerInvokerFactory returns same instance
    14. resetWorkerInvokerFactory allows new instance

    IMPORTANT: Call resetWorkerInvokerFactory() in beforeEach to clean up singleton state between tests.
  </action>
  <verify>npm test -- WorkerInvokerFactory.test.ts passes all tests</verify>
  <done>All 14 test cases pass, factory registration and lookup verified</done>
</task>

<task type="auto">
  <name>Task 3: Create LambdaInvoker unit tests</name>
  <files>platform/backend/src/__tests__/unit/workers/invokers/LambdaInvoker.test.ts</files>
  <action>
    Create test file at platform/backend/src/__tests__/unit/workers/invokers/LambdaInvoker.test.ts

    Mock AWS SDK v3 Lambda client following established pattern from AwsSsmProvider.test.ts:
    ```typescript
    jest.mock('@aws-sdk/client-lambda', () => {
      return {
        LambdaClient: jest.fn().mockImplementation(() => ({
          send: jest.fn(),
        })),
        InvokeCommand: jest.fn().mockImplementation((input) => ({ input })),
      };
    });
    ```

    Create mock job data:
    ```typescript
    const mockJob: JobData = {
      id: 'job-123',
      tenantId: 'tenant-1',
      repository: 'https://github.com/test/repo',
      task: 'Fix the bug',
      branch: null,
      baseBranch: 'main',
      context: {},
      settings: {},
      timestamp: Date.now(),
    };
    ```

    Test cases for invoke():
    1. Returns executionId (requestId from $metadata) on success
    2. Returns workerType='lambda' in result
    3. Builds correct payload with all job fields
    4. Uses InvocationType='Event' for async invocation
    5. Logs invocation with jobId, functionName, executionId

    Test cases for error classification:
    6. Throws permanent error when functionName not in deploymentConfig
    7. Classifies TooManyRequestsException as TRANSIENT
    8. Classifies ServiceException as TRANSIENT
    9. Classifies 5xx status codes as TRANSIENT
    10. Classifies ResourceNotFoundException as PERMANENT
    11. Classifies InvalidParameterException as PERMANENT

    Test cases for isAvailable():
    12. Returns true when client is initialized

    Get mockSend from private instance in beforeEach:
    ```typescript
    const invoker = new LambdaInvoker();
    const mockClient = invoker['client'] as any;
    mockSend = mockClient.send;
    ```

    For success response mock:
    ```typescript
    mockSend.mockResolvedValueOnce({
      $metadata: { requestId: 'test-request-id' },
      StatusCode: 202,
    });
    ```
  </action>
  <verify>npm test -- LambdaInvoker.test.ts passes all tests</verify>
  <done>All 12 test cases pass, Lambda invocation and error classification verified</done>
</task>

</tasks>

<verification>
- All test files created in correct directories
- Tests follow existing patterns from credential system
- AWS SDK mocking uses jest.mock() pattern
- Factory reset called in beforeEach
- npm test passes for all three test files
</verification>

<success_criteria>
- WorkerError.test.ts: 7 tests passing
- WorkerInvokerFactory.test.ts: 14 tests passing
- LambdaInvoker.test.ts: 12 tests passing
- Total: 33 tests passing
- Error classification logic verified
- Factory registration and lookup verified
- Lambda async invocation verified
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-testing/04.2-01-SUMMARY.md`
</output>
