---
phase: 11-deployment-process
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/frontend/next.config.mjs
  - platform/frontend/package.json
  - platform/frontend/.env.production.example
autonomous: true

must_haves:
  truths:
    - "Frontend builds to static export (out/ directory)"
    - "Static assets are optimized for S3 hosting"
    - "API URL is configurable via environment variable"
    - "Build fails if API URL is not provided"
    - "No server-side rendering dependencies remain"
  artifacts:
    - path: "platform/frontend/next.config.mjs"
      provides: "Next.js static export configuration"
      contains: ["output: 'export'"]
    - path: "platform/frontend/package.json"
      provides: "Build scripts for static export"
      contains: ["export"]
    - path: "platform/frontend/.env.production.example"
      provides: "Production environment variable template"
      contains: ["NEXT_PUBLIC_API_URL"]
  key_links:
    - from: "next build"
      to: "platform/frontend/out"
      via: "static export"
      pattern: "output.*export"
    - from: "S3 sync"
      to: "CloudFront"
      via: "invalidation after upload"
      pattern: "aws s3 sync"
---

<objective>
Configure Next.js frontend for static site deployment to S3+CloudFront with environment-specific API URL configuration.

Purpose: Next.js default is SSR with Node.js server. S3+CloudFront requires static export with all configuration baked in at build time.

Output: Frontend builds to static out/ directory ready for S3 upload
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-deployment-process/11-RESEARCH.md
@.planning/phases/10-aws-infrastructure/10-08-SUMMARY.md
@.planning/phases/10-aws-infrastructure/10-09-SUMMARY.md

@platform/frontend/package.json
@platform/frontend/next.config.mjs
@infrastructure/components/frontend.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Next.js for static export</name>
  <files>platform/frontend/next.config.mjs</files>
  <action>
    Update next.config.mjs to enable static export:
    1. Add `output: 'export'` configuration
    2. Add `images: { unoptimized: true }` (S3 static hosting doesn't support Next.js Image Optimization API)
    3. Keep trailingSlash handling for SPA routing

    Current config is empty. Add:
    ```javascript
    /** @type {import('next').NextConfig} */
    const nextConfig = {
      output: 'export',
      images: {
        unoptimized: true,
      },
      trailingSlash: true,
    }

    export default nextConfig
    ```

    Note: CloudFront handles SPA routing via error response custom error pages (configured in Phase 10).
  </action>
  <verify>
    npm run build -w @viberator/frontend
    Verify out/ directory is created with static files
  </verify>
  <done>
  Frontend builds to static out/ directory with all assets
  </done>
</task>

<task type="auto">
  <name>Task 2: Add build and export scripts to package.json</name>
  <files>platform/frontend/package.json</files>
  <action>
    Add export script if not present:
    1. Ensure "build": "next build" exists (it does)
    2. Add optional "export": "next build" (alias for CI/CD clarity)
    3. Verify no server-side scripts conflict with static export

    Current scripts:
    - "dev": "next dev" (unchanged)
    - "build": "next build" (unchanged - now produces static export)
    - "start": "next start" - this will FAIL with static export, keep for local dev understanding
  </action>
  <verify>
    grep -E "(build|export)" platform/frontend/package.json
  </verify>
  <done>
  Build script produces static export compatible with S3 hosting
  </done>
</task>

<task type="auto">
  <name>Task 3: Create production environment variable template</name>
  <files>platform/frontend/.env.production.example</files>
  <action>
    Create .env.production.example with:
    - NEXT_PUBLIC_API_URL - Backend API URL (from ALB or CloudFront)
    - NEXT_PUBLIC_CDN_URL - Optional: CloudFront URL for frontend
    - Documentation comments for each variable

    Template:
    ```
    # Backend API URL - should match backendUrl from Pulumi stack output
    # Example: https://abc123.us-east-1.elb.amazonaws.com or custom domain
    NEXT_PUBLIC_API_URL=

    # Frontend CDN URL - optional, for asset references
    # Example: https://d111111abcdef8.cloudfront.net
    NEXT_PUBLIC_CDN_URL=
    ```
  </action>
  <verify>
    cat platform/frontend/.env.production.example
  </verify>
  <done>
  Environment variable template documents required production configuration
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify no dynamic API routes break static export</name>
  <files>platform/frontend/src/app</files>
  <action>
    Check for Next.js API routes (src/app/api/*):
    1. List all files in src/app/api/
    2. If any exist, note that API routes don't work with static export
    3. Document if frontend has any API routes that need to be removed or moved to backend

    Static export constraint: Next.js API routes are not supported with output: 'export'.

    If API routes exist, either:
    - Move to backend (recommended for consistency)
    - Use API route alias if needed (but static export won't include them)
  </action>
  <verify>
    find platform/frontend/src -type f -path "*/api/*" -name "*.ts" -o -path "*/api/*" -name "*.tsx"
  </verify>
  <done>
  All API routes identified and documented or removed for static export compatibility
  </done>
</task>

</tasks>

<verification>
1. Static export builds: `npm run build -w @viberator/frontend`
2. out/ directory exists with index.html
3. out/_next/static/ exists with hashed assets
4. Build fails without NEXT_PUBLIC_API_URL set (optional strict mode)
</verification>

<success_criteria>
1. Frontend builds to static out/ directory
2. No server-side dependencies in output
3. API URL configurable via environment variable
4. Static assets are properly hashed for caching
5. Build is compatible with S3+CloudFront deployment from Phase 10
</success_criteria>

<output>
After completion, create `.planning/phases/11-deployment-process/11-02-SUMMARY.md`
</output>
