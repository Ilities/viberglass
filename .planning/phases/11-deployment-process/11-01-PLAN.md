---
phase: 11-deployment-process
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/backend/Dockerfile.prod
  - platform/backend/.dockerignore
autonomous: true
user_setup:
  - service: aws
    why: "Container registry for backend images"
    env_vars:
      - name: AWS_ROLE_ARN
        source: "GitHub Actions OIDC role ARN (created in Phase 10 or manually)"

must_haves:
  truths:
    - "Production Docker image builds successfully from monorepo root"
    - "Docker image uses production-optimized base (node:20-alpine not node:24-alpine)"
    - "TypeScript compilation happens during build, not at runtime"
    - "Image size is minimized via multi-stage build"
    - "Health endpoint at /health responds to container health checks"
  artifacts:
    - path: "platform/backend/Dockerfile.prod"
      provides: "Production Docker image definition"
      min_lines: 30
    - path: "platform/backend/.dockerignore"
      provides: "Build context optimization"
      contains: ["node_modules", ".git", "dist"]
  key_links:
    - from: "Dockerfile.prod"
      to: "ECR"
      via: "docker push in CI/CD"
      pattern: "FROM.*alpine.*AS"
    - from: "ECS health check"
      to: "container /health endpoint"
      via: "ALB health checks"
      pattern: "CMD-SHELL.*curl.*health"
---

<objective>
Create production-optimized Dockerfile for backend ECS deployment with multi-stage build, TypeScript compilation, and health endpoint integration.

Purpose: The existing Dockerfile uses tsx for development (slow startup, larger image). Production ECS needs compiled code, minimal layers, and fast startup for health check compliance.

Output: Dockerfile.prod ready for ECR push and ECS deployment
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-deployment-process/11-RESEARCH.md
@.planning/phases/10-aws-infrastructure/10-07-SUMMARY.md
@.planning/phases/10-aws-infrastructure/10-09-SUMMARY.md

@platform/backend/package.json
@platform/backend/Dockerfile
@infrastructure/components/backend-ecs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-stage production Dockerfile</name>
  <files>platform/backend/Dockerfile.prod</files>
  <action>
    Create multi-stage Dockerfile with:
    1. Build stage: node:20-alpine (matches project engines requirement)
    2. Copy workspace package files (root, packages/types, platform/backend)
    3. Run npm ci --legacy-peer-deps (workspace compatibility)
    4. Build @viberglass/types first, then @viberator/platform-backend
    5. Production stage: node:20-alpine
    6. Copy compiled dist from build stage
    7. Copy only production node_modules
    8. Set NODE_ENV=production
    9. Expose port 3000
    10. HEALTHCHECK instruction using curl (wget if curl not available)
    11. CMD ["node", "dist/api/server.js"]

    Do NOT use tsx for production. Do NOT use node:24 (project requires node:20).
    The backend package.json shows "start": "node ./dist/api/server.js" - use this.
  </action>
  <verify>
    docker build -f platform/backend/Dockerfile.prod -t viberator-backend:test .
    Verify image builds and layers are minimal (docker history)
  </verify>
  <done>
  Production Dockerfile builds successfully with TypeScript pre-compiled, minimal image size, and health check instruction
  </done>
</task>

<task type="auto">
  <name>Task 2: Create optimized .dockerignore for backend</name>
  <files>platform/backend/.dockerignore</files>
  <action>
    Create .dockerignore that excludes:
    - node_modules (rebuild in container for platform consistency)
    - .git
    - dist (build output, not needed in build context)
    - *.log
    - .env (use SSM in production)
    - coverage
    - .vscode
    - .idea
    - Dockerfile*
    - docker-compose*
    - migrations (not needed in container)

    Existing .dockerignore exists - update it for production optimization if needed.
  </action>
  <verify>
    cat platform/backend/.dockerignore
    Verify it excludes development artifacts and unnecessary files
  </verify>
  <done>
  .dockerignore reduces build context by excluding unnecessary files
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify /health endpoint exists and responds correctly</name>
  <files>platform/backend/src/api/server.ts</files>
  <action>
    Verify the Express server has a /health endpoint that:
    1. Returns HTTP 200 when service is healthy
    2. Optionally checks database connectivity
    3. Returns JSON response

    Search for existing health endpoint in server.ts or routes. If missing, add simple endpoint:
    ```typescript
    app.get('/health', (req, res) => {
      res.status(200).json({ status: 'ok', timestamp: new Date().toISOString() });
    });
    ```

    The ECS component (from Phase 10) expects health check on /health with:
    - interval: 30 seconds
    - timeout: 5 seconds
    - retries: 3
    - startPeriod: 60 seconds
  </action>
  <verify>
    grep -n "health" platform/backend/src/api/server.ts
    Or grep -rn "health" platform/backend/src/api/routes/
  </verify>
  <done>
  /health endpoint returns HTTP 200 with JSON response for ALB health checks
  </done>
</task>

</tasks>

<verification>
1. Image builds from monorepo root: `docker build -f platform/backend/Dockerfile.prod -t test .`
2. Image uses node:20-alpine base: `docker inspect test | grep Alp`
3. Entry point is compiled JavaScript: `docker inspect test | grep Cmd`
4. Health check instruction present: `docker inspect test | grep Health`
</verification>

<success_criteria>
1. Production Docker image builds successfully from monorepo root
2. Image size < 500MB (multi-stage optimization)
3. Health check instruction configured for container orchestration
4. No tsx or development dependencies in production image
5. TypeScript compilation happens during build, not runtime
</success_criteria>

<output>
After completion, create `.planning/phases/11-deployment-process/11-01-SUMMARY.md`
</output>
