---
phase: 14-code-and-ui-branding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/frontend/src/app/(app)/clankers/new/page.tsx
  - platform/frontend/src/components/webhook-config-form.tsx
autonomous: true

must_haves:
  truths:
    - "UI text references 'Viberators' when describing worker agents"
    - "Placeholder text uses correct Viberator terminology"
  artifacts:
    - path: platform/frontend/src/app/(app)/clankers/new/page.tsx
      provides: "Clanker creation page UI"
      contains: "Viberators"
    - path: platform/frontend/src/components/webhook-config-form.tsx
      provides: "Webhook configuration form component"
      contains: "viberator-bot"
  key_links:
    - from: platform/frontend/src/app/(app)/clankers/new/page.tsx
      to: "User reading clanker description"
      via: "Subheading text content"
      pattern: "Viberators"
---

<objective>
Update frontend UI text to reference "Viberators" when describing worker/agent components. This clarifies that Clankers configure Viberator workers.

Purpose: Users understand that Clankers are configuration files for Viberator workers, maintaining the branding split where platform = Viberglass, workers = Viberators.

Output: Updated UI descriptions using "Viberators" terminology for worker references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-code-and-ui-branding/14-RESEARCH.md

@platform/frontend/src/app/(app)/clankers/new/page.tsx
@platform/frontend/src/components/webhook-config-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Update clanker creation page to reference Viberators</name>
  <files>platform/frontend/src/app/(app)/clankers/new/page.tsx</files>
  <action>
    Find the Subheading component describing clankers (around line 112) and update the text to use "Viberators" for worker references:

    From: "Configure a new agent worker for your viberator tasks."
    To: "Configure a new agent worker for your Viberator tasks."

    The key change is capitalizing "Viberator" to "Viberator" as it's the proper name of the worker system.

    DO NOT change:
    - The term "Clanker" (this is the entity name for worker configurations)
    - "agent worker" terminology
    - Any other UI text
  </action>
  <verify>grep -i "Viberator" platform/frontend/src/app/\(app\)/clankers/new/page.tsx shows "Viberator tasks" with capital V</verify>
  <done>Clanker description references "Viberator tasks" with proper capitalization</done>
</task>

<task type="auto">
  <name>Verify webhook placeholder uses consistent Viberator terminology</name>
  <files>platform/frontend/src/components/webhook-config-form.tsx</files>
  <action>
    The webhook config form uses "viberator-bot" as a placeholder example for bot username detection (around line 279-284).

    Change the placeholder text from lowercase to properly capitalized:
    From: `placeholder="viberator-bot"`
    To: `placeholder="viberglass-bot"`

    This aligns the example with the new platform branding while maintaining the utility of the placeholder as an example.

    DO NOT change:
    - The label text ("Username for bot mention detection")
    - The help text description
    - Any other form fields
  </action>
  <verify>grep "placeholder=" platform/frontend/src/components/webhook-config-form.tsx shows "viberglass-bot" in the username field</verify>
  <done>Bot username placeholder example uses "viberglass-bot"</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build the frontend: `npm run build -w @viberator/frontend`
2. Verify no TypeScript errors
3. Confirm text changes are grammatically correct in context
</verification>

<success_criteria>
1. Clanker creation page describes "Viberator tasks" (proper capitalization)
2. Webhook placeholder example uses "viberglass-bot"
3. Frontend builds successfully with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-and-ui-branding/14-03-SUMMARY.md` with:
- Files modified: clankers/new/page.tsx, webhook-config-form.tsx
- Verification results of terminology updates
- Any issues encountered
</output>
