---
phase: 14-code-and-ui-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/frontend/src/app/layout.tsx
  - platform/frontend/src/app/logo.tsx
  - platform/frontend/branding.md
autonomous: true

must_haves:
  truths:
    - "Browser tab title shows 'Viberglass' not 'Viberator'"
    - "Logo SVG displays 'VIBERGLASS' not 'VIBERATOR'"
    - "Branding guidelines document references Viberglass platform"
  artifacts:
    - path: platform/frontend/src/app/layout.tsx
      provides: "Frontend metadata and page titles"
      contains: "Viberglass"
    - path: platform/frontend/src/app/logo.tsx
      provides: "SVG logo component with wordmark"
      contains: "VIBERGLASS"
    - path: platform/frontend/branding.md
      provides: "Brand concept and color palette documentation"
      min_lines: 50
  key_links:
    - from: platform/frontend/src/app/layout.tsx
      to: "Browser tab/title"
      via: "Next.js Metadata API"
      pattern: "title.*Viberglass"
    - from: platform/frontend/src/app/logo.tsx
      to: "Logo display"
      via: "SVG text element"
      pattern: "VIBERGLASS"
---

<objective>
Update user-facing frontend UI to display "Viberglass" as the platform name instead of "Viberator." This covers page titles, metadata, and the inline logo component.

Purpose: Users see the correct platform branding in browser tabs, window titles, and the application logo. This is the first visible change in the branding update.

Output: Updated layout.tsx metadata, updated logo.tsx wordmark, updated branding guidelines document.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-code-and-ui-branding/14-RESEARCH.md

@platform/frontend/src/app/layout.tsx
@platform/frontend/src/app/logo.tsx
@platform/frontend/branding.md
</context>

<tasks>

<task type="auto">
  <name>Update frontend layout metadata to use Viberglass</name>
  <files>platform/frontend/src/app/layout.tsx</files>
  <action>
    In the metadata object, replace all instances of "Viberator" with "Viberglass":

    1. Change `template: '%s - Viberator'` to `template: '%s - Viberglass'`
    2. Change `default: 'Viberator'` to `default: 'Viberglass'`

    DO NOT change:
    - The description text (it's generic: "AI-powered bug fixing orchestrator")
    - Font configurations
    - Any other metadata fields
  </action>
  <verify>grep -n "Viberglass" platform/frontend/src/app/layout.tsx shows 2 matches; grep -n "Viberator" platform/frontend/src/app/layout.tsx shows 0 matches (except in comments if any)</verify>
  <done>Metadata title template and default both use "Viberglass"</done>
</task>

<task type="auto">
  <name>Update logo SVG wordmark to display VIBERGLASS</name>
  <files>platform/frontend/src/app/logo.tsx</files>
  <action>
    In the SVG text element within the Wordmark section, change the text content from "VIBERATOR" to "VIBERGLASS":

    Find the line:
    ```typescript
    <text x="30" y="20" ...>VIBERATOR</text>
    ```

    Change to:
    ```typescript
    <text x="30" y="20" ...>VIBERGLASS</text>
    ```

    DO NOT change:
    - SVG viewBox or dimensions
    - Gradient definitions
    - Icon/graphics (the wave and baton elements)
    - Font styling attributes
  </action>
  <verify>grep "VIBERGLASS" platform/frontend/src/app/logo.tsx shows the wordmark text; grep "VIBERATOR" platform/frontend/src/app/logo.tsx returns no results</verify>
  <done>Logo wordmark displays "VIBERGLASS"</done>
</task>

<task type="auto">
  <name>Update branding guidelines document with Viberglass brand concept</name>
  <files>platform/frontend/branding.md</files>
  <action>
    Update the brand concept section (near end of file) to reflect the new platform name:

    1. Update the heading from "# Viberator Project Branding Guidelines" to "# Viberglass Project Branding Guidelines"
    2. Update the brand concept paragraph (around line 154) from:
       "Viberator is an orchestrator - the name combines 'vibe' (harmony, coordination) with 'orchestrator'..."
       to:
       "Viberglass is an orchestrator platform - the name combines 'vibe' (harmony, coordination) with 'glass' (transparency, clarity)..."

    Keep the color palette and all design specifications unchanged - these are still valid.
  </action>
  <verify>head -n 5 platform/frontend/branding.md shows "Viberglass Project Branding Guidelines"; grep -c "Viberglass" platform/frontend/branding.md is at least 2</verify>
  <done>Branding document header and concept describe Viberglass platform</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build the frontend: `npm run build -w @viberator/frontend`
2. Verify no TypeScript errors
3. Check that grep for "Viberator" in the three modified files returns no results (except import paths)
</verification>

<success_criteria>
1. Browser tab displays "Viberglass" as platform name when viewing any page
2. Logo component renders "VIBERGLASS" wordmark
3. Branding guidelines document describes Viberglass brand concept
4. Frontend builds successfully with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-and-ui-branding/14-01-SUMMARY.md` with:
- Files modified: layout.tsx, logo.tsx, branding.md
- Verification results of branding changes
- Any issues encountered
</output>
