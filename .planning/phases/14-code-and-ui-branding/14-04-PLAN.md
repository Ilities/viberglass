---
phase: 14-code-and-ui-branding
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - platform/backend/README.md
  - infrastructure/README.md
autonomous: true

must_haves:
  truths:
    - "Backend README references Viberglass platform and Viberator workers correctly"
    - "Infrastructure README uses Viberglass for platform references"
  artifacts:
    - path: platform/backend/README.md
      provides: "Backend service documentation"
      min_lines: 50
    - path: infrastructure/README.md
      provides: "Infrastructure documentation"
      min_lines: 100
  key_links:
    - from: platform/backend/README.md
      to: "Developer understanding backend integration"
      via: "Documentation text"
      pattern: "Viberglass platform.*Viberator workers"
---

<objective>
Update backend and infrastructure README files to correctly distinguish between Viberglass (platform) and Viberators (workers). These documentation files weren't covered in Phase 13.

Purpose: Developers reading backend and infrastructure documentation understand the branding split consistently. Platform references use "Viberglass", worker references use "Viberator".

Output: Updated README.md files with consistent branding terminology.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-code-and-ui-branding/14-RESEARCH.md

@platform/backend/README.md
@infrastructure/README.md
</context>

<tasks>

<task type="auto">
  <name>Update backend README branding terminology</name>
  <files>platform/backend/README.md</files>
  <action>
    Update the backend README to use consistent branding:

    1. Line 93: Change "powered by the Viberator worker service" to "powered by Viberator workers"
    2. Line 124: Change "the Viberator worker service" to "Viberator workers"

    The pattern is:
    - Platform = "Viberglass"
    - Workers = "Viberator" (singular, the worker type)
    - Multiple workers = "Viberators"

    DO NOT change:
    - File paths like "viberator/app/" (these are actual directory names)
    - Code examples with package names (@viberator/*)
    - SSM paths (/viberator/*) - these reference the worker subsystem
  </action>
  <verify>grep -n "Viberator worker service" platform/backend/README.md returns no results; grep "Viberator workers" platform/backend/README.md shows updated text</verify>
  <done>Backend README uses "Viberator workers" terminology correctly</done>
</task>

<task type="auto">
  <name>Update infrastructure README header and description</name>
  <files>infrastructure/README.md</files>
  <action>
    Update the infrastructure README header and opening description:

    1. Line 1: Change "# Viberator AWS Infrastructure" to "# Viberglass AWS Infrastructure"
    2. Line 3: Change "Pulumi-based AWS infrastructure for Viberator" to "Pulumi-based AWS infrastructure for Viberglass"

    This changes the platform references while keeping worker/agent references as "Viberator".

    DO NOT change:
    - SSM parameter paths (/viberator/*) - these reference the worker subsystem and are correct
    - Pulumi stack names (will change in Phase 15)
    - Container names (will change in Phase 15)
    - CloudWatch log paths (/viberator/*) - these reference worker subsystem
  </action>
  <verify>head -n 5 infrastructure/README.md shows "Viberglass" in title and description</verify>
  <done>Infrastructure README header describes Viberglass platform</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Review both files to ensure branding is consistent
2. Verify no broken references to file paths
3. Confirm terminology distinguishes platform (Viberglass) from workers (Viberator)
</verification>

<success_criteria>
1. Backend README says "Viberator workers" not "Viberator worker service"
2. Infrastructure README header shows "Viberglass AWS Infrastructure"
3. All changes maintain path references to actual directories (/viberator/*)
4. Documentation is consistent with branding split
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-and-ui-branding/14-04-SUMMARY.md` with:
- Files modified: backend and infrastructure READMEs
- Summary of branding changes made
- Any path references preserved (not changed)
</output>
