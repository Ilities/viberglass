---
phase: 14-code-and-ui-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/backend/src/webhooks/providers/base-provider.ts
  - platform/backend/src/webhooks/providers/github-provider.ts
autonomous: true

must_haves:
  truths:
    - "Webhook User-Agent header sends 'Viberglass-Webhook/1.0' to external services"
    - "GitHub webhook provider User-Agent uses 'Viberglass-Webhook/1.0'"
  artifacts:
    - path: platform/backend/src/webhooks/providers/base-provider.ts
      provides: "Base webhook provider with HTTP client"
      contains: "Viberglass-Webhook"
    - path: platform/backend/src/webhooks/providers/github-provider.ts
      provides: "GitHub-specific webhook provider implementation"
      contains: "Viberglass-Webhook"
  key_links:
    - from: platform/backend/src/webhooks/providers/base-provider.ts
      to: "External webhook services"
      via: "axios HTTP request User-Agent header"
      pattern: "Viberglass-Webhook/1.0"
    - from: platform/backend/src/webhooks/providers/github-provider.ts
      to: "GitHub API"
      via: "axios HTTP request User-Agent header"
      pattern: "Viberglass-Webhook/1.0"
---

<objective>
Update backend API to use "Viberglass" in platform-referencing strings. This covers webhook User-Agent headers sent to external services.

Purpose: External services receiving webhooks (GitHub, etc.) see "Viberglass-Webhook" in their logs instead of "Viberator-Webhook". This maintains consistent branding across API integrations.

Output: Updated webhook provider User-Agent headers using Viberglass naming.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-code-and-ui-branding/14-RESEARCH.md

@platform/backend/src/webhooks/providers/base-provider.ts
@platform/backend/src/webhooks/providers/github-provider.ts
</context>

<tasks>

<task type="auto">
  <name>Update base webhook provider User-Agent header</name>
  <files>platform/backend/src/webhooks/providers/base-provider.ts</files>
  <action>
    Find the User-Agent header configuration (around line 335 in the HTTP client setup) and change:

    From: `'User-Agent': 'Viberator-Webhook/1.0'`
    To: `'User-Agent': 'Viberglass-Webhook/1.0'`

    This header is used when making HTTP requests to external webhook endpoints. Look for the axios configuration or headers object where User-Agent is set.
  </action>
  <verify>grep "Viberglass-Webhook" platform/backend/src/webhooks/providers/base-provider.ts shows the User-Agent value; grep "Viberator-Webhook" returns no results</verify>
  <done>User-Agent header uses "Viberglass-Webhook/1.0"</done>
</task>

<task type="auto">
  <name>Update GitHub webhook provider User-Agent header</name>
  <files>platform/backend/src/webhooks/providers/github-provider.ts</files>
  <action>
    Find the User-Agent header configuration (around line 333 in the HTTP client setup) and change:

    From: `'User-Agent': 'Viberator-Webhook/1.0'`
    To: `'User-Agent': 'Viberglass-Webhook/1.0'`

    This header is used when making HTTP requests to GitHub's API. Look for the axios configuration or headers object where User-Agent is set.
  </action>
  <verify>grep "Viberglass-Webhook" platform/backend/src/webhooks/providers/github-provider.ts shows the User-Agent value; grep "Viberator-Webhook" returns no results</verify>
  <done>GitHub provider User-Agent uses "Viberglass-Webhook/1.0"</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build the backend: `npm run build -w @viberator/platform-backend`
2. Verify no TypeScript errors
3. Confirm User-Agent string changes in both files
</verification>

<success_criteria>
1. Base webhook provider sends "Viberglass-Webhook/1.0" User-Agent
2. GitHub webhook provider sends "Viberglass-Webhook/1.0" User-Agent
3. Backend builds successfully with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-and-ui-branding/14-02-SUMMARY.md` with:
- Files modified: base-provider.ts, github-provider.ts
- Verification results of User-Agent changes
- Any issues encountered
</output>
