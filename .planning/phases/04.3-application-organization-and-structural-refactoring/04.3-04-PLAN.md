---
phase: 04.3-application-organization-and-structural-refactoring
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - viberator/app/src/index.ts
  - viberator/app/src/config/ConfigManager.ts
  - viberator/app/src/agents/BaseAgent.ts
  - viberator/app/src/workers/viberator.ts
  - platform/backend/src/services/JobService.ts
  - platform/backend/src/api/server.ts
autonomous: true

must_haves:
  truths:
    - "Logger type is properly defined instead of 'any'"
    - "Configuration types are properly defined"
    - "No 'as any' casts remain for typeable values"
  artifacts:
    - path: viberator/app/src/index.ts
      not_contains: "as any"
    - path: viberator/app/src/config/ConfigManager.ts
      not_contains: "as any"
    - path: viberator/app/src/agents/BaseAgent.ts
      not_contains: "as any"
  key_links:
    - from: "All modified files"
      to: "Proper type definitions"
      via: "TypeScript types"
      pattern: ": (Logger|Config|Type)"
---

<objective>
Replace 'as any' type casts with proper type definitions in high-traffic files.

Purpose: Improve type safety and enable better IDE support and compile-time error detection.
Output: Properly typed code with minimal 'as any' casts remaining.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/04.3-application-organization-and-structural-refactoring/04.3-RESEARCH.md

@viberator/app/src/index.ts
@viberator/app/src/config/ConfigManager.ts
@viberator/app/src/agents/BaseAgent.ts
@viberator/app/src/workers/viberator.ts
@platform/backend/src/services/JobService.ts
@platform/backend/src/api/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 'as any' in viberator/app/src/index.ts</name>
  <files>viberator/app/src/index.ts</files>
  <action>
    Find and fix the 'as any' cast around line 10 and line 329.

    For each occurrence:
    1. Read the context to understand what type is actually needed
    2. Define a proper type or use an existing type from the codebase
    3. Replace 'as any' with the proper type

    Common patterns to fix:
    - If it's a logger: Import Logger from 'winston' and type it properly
    - If it's a config object: Define an interface for the config shape
    - If it's an Express request/response: Use proper Express types

    DO NOT:
    - Change function behavior
    - Add unnecessary complexity for truly dynamic data

    DO:
    - Add missing type definitions where sensible
    - Import types from existing packages (winston, express, etc.)
  </action>
  <verify>
    grep -n "as any" viberator/app/src/index.ts returns no results (or only truly unavoidable cases)
    TypeScript compilation passes
  </verify>
  <done>
    - 'as any' casts replaced with proper types
    - Code compiles without type errors
    - No functional changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix 'as any' in viberator/app/src/config/ConfigManager.ts</name>
  <files>viberator/app/src/config/ConfigManager.ts</files>
  <action>
    Find the 'as any' cast around line 2.

    The pattern is likely for environment variable access or config parsing.
    1. If it's for process.env: Define a proper config interface
    2. If it's for parsed config: Add type guards or proper validation
    3. Consider using a schema validation library (like Joi, already in backend deps) if appropriate

    Replace with proper typing.
  </action>
  <verify>
    grep -n "as any" viberator/app/src/config/ConfigManager.ts returns no results
    TypeScript compilation passes
  </verify>
  <done>
    - 'as any' cast replaced with proper types
    - Config access is type-safe
    - Code compiles without type errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix 'as any' in viberator/app/src/agents/BaseAgent.ts</name>
  <files>viberator/app/src/agents/BaseAgent.ts</files>
  <action>
    Find the 'as any' casts (2 occurrences).

    These are likely in the context of agent execution results or config.
    1. Identify what the actual type should be
    2. Define proper types if they don't exist
    3. Replace 'as any' with proper types

    Consider adding a shared ExecutionResult type if not already defined.
  </action>
  <verify>
    grep -n "as any" viberator/app/src/agents/BaseAgent.ts returns no results
    TypeScript compilation passes
  </verify>
  <done>
    - 'as any' casts replaced with proper types
    - Agent execution is properly typed
    - Code compiles without type errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix 'as any' in viberator/app/src/workers/viberator.ts</name>
  <files>viberator/app/src/workers/viberator.ts</files>
  <action>
    Find and fix the 4 occurrences of 'as any'.

    These are likely related to worker payload handling.
    1. Check if WorkerPayload types exist (they should from Phase 3)
    2. Use discriminated union types instead of 'as any'
    3. Add type guards for runtime validation if needed

    Reference existing types from @viberglass/types if available.
  </action>
  <verify>
    grep -n "as any" viberator/app/src/workers/viberator.ts returns no results
    TypeScript compilation passes
  </verify>
  <done>
    - 'as any' casts replaced with proper WorkerPayload types
    - Worker execution is properly typed
    - Code compiles without type errors
  </done>
</task>

<task type="auto">
  <name>Task 5: Fix 'as any' in platform/backend files</name>
  <files>platform/backend/src/services/JobService.ts, platform/backend/src/api/server.ts</files>
  <action>
    Find and fix 'as any' casts in these backend files:
    1. JobService.ts - 1 occurrence
    2. server.ts - 2 occurrences (likely in DEBUG_LOG sections)

    For JobService: Check if it's related to job status or progress types.
    For server.ts: Check if it's logger or Express-related types.

    Replace with proper types.
  </action>
  <verify>
    grep -n "as any" platform/backend/src/services/JobService.ts returns no results
    grep -n "as any" platform/backend/src/api/server.ts returns no results
    TypeScript compilation passes
  </verify>
  <done>
    - 'as any' casts replaced with proper types
    - Backend code is properly typed
    - Code compiles without type errors
  </done>
</task>

</tasks>

<verification>
1. npm run build compiles successfully in all packages
2. grep -r "as any" shows significant reduction (ideally 0 in source files)
3. No runtime errors from type changes
4. Existing tests pass
</verification>

<success_criteria>
1. 'as any' count reduced from 58 to <10 (only truly unavoidable cases)
2. All high-traffic files properly typed
3. TypeScript strict mode passes
4. No functional regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-application-organization-and-structural-refactoring/04.3-04-SUMMARY.md`
</output>
