---
phase: 04.3-application-organization-and-structural-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - viberator/app/src/orchestrator/AgentOrchestrator.ts
  - platform/backend/src/api/routes/jobs.ts
  - platform/backend/src/api/routes/tickets.ts
autonomous: true

must_haves:
  truths:
    - "No substr() calls remain in codebase"
    - "All ID generation uses crypto.randomUUID()"
    - "Code uses modern non-deprecated APIs"
  artifacts:
    - path: viberator/app/src/orchestrator/AgentOrchestrator.ts
      contains: "crypto.randomUUID()"
    - path: platform/backend/src/api/routes/jobs.ts
      contains: "crypto.randomUUID()"
    - path: platform/backend/src/api/routes/tickets.ts
      contains: "crypto.randomUUID()"
  key_links:
    - from: "AgentOrchestrator.ts"
      to: "crypto module"
      via: "import { randomUUID } from 'crypto'"
      pattern: "from 'crypto'"
---

<objective>
Replace deprecated String.substr() calls with crypto.randomUUID() for ID generation.

Purpose: Remove deprecated APIs, use cryptographically secure random IDs, improve code quality.
Output: Modern ID generation using Node.js crypto.randomUUID() with no deprecated substr() calls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/04.3-application-organization-and-structural-refactoring/04.3-RESEARCH.md

@viberator/app/src/orchestrator/AgentOrchestrator.ts
@platform/backend/src/api/routes/jobs.ts
@platform/backend/src/api/routes/tickets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace deprecated substr() in AgentOrchestrator.ts</name>
  <files>viberator/app/src/orchestrator/AgentOrchestrator.ts</files>
  <action>
    Find the line around line 160:
    ```typescript
    const executionId = `exec_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    ```

    Replace with:
    ```typescript
    import { randomUUID } from 'crypto';
    // ... elsewhere in code ...
    const executionId = `exec_${Date.now()}_${randomUUID().slice(0, 8)}`;
    ```

    Changes:
    1. Add `import { randomUUID } from 'crypto';` at top of file (check if crypto already imported)
    2. Replace the substr() call with randomUUID().slice(0, 8)
    3. The slice(0, 8) gives us 8 characters which is similar to the original 9-character substr output

    Do NOT change the executionId format structure - keep the `exec_${Date.now()}_` prefix.
  </action>
  <verify>
    grep -n "substr(" viberator/app/src/orchestrator/AgentOrchestrator.ts returns no results
    grep -n "randomUUID" viberator/app/src/orchestrator/AgentOrchestrator.ts shows the new usage
  </verify>
  <done>
    - executionId generation uses crypto.randomUUID().slice(0, 8)
    - No substr() calls remain in AgentOrchestrator.ts
    - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace deprecated substr() in jobs.ts route</name>
  <files>platform/backend/src/api/routes/jobs.ts</files>
  <action>
    Find the line around line 28:
    ```typescript
    const jobId = `job_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    ```

    Replace with:
    ```typescript
    import { randomUUID } from 'crypto';
    // ... elsewhere in code ...
    const jobId = `job_${Date.now()}_${randomUUID().slice(0, 8)}`;
    ```

    Changes:
    1. Add `import { randomUUID } from 'crypto';` at top of file
    2. Replace the substr() call with randomUUID().slice(0, 8)

    Do NOT change the jobId format structure.
  </action>
  <verify>
    grep -n "substr(" platform/backend/src/api/routes/jobs.ts returns no results
    grep -n "randomUUID" platform/backend/src/api/routes/jobs.ts shows the new usage
  </verify>
  <done>
    - jobId generation uses crypto.randomUUID().slice(0, 8)
    - No substr() calls remain in jobs.ts
    - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace deprecated substr() in tickets.ts route</name>
  <files>platform/backend/src/api/routes/tickets.ts</files>
  <action>
    Find the line around line 332:
    ```typescript
    const jobId = `job_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    ```

    Replace with:
    ```typescript
    import { randomUUID } from 'crypto';
    // ... elsewhere in code ...
    const jobId = `job_${Date.now()}_${randomUUID().slice(0, 8)}`;
    ```

    Changes:
    1. Add `import { randomUUID } from 'crypto';` at top of file
    2. Replace the substr() call with randomUUID().slice(0, 8)

    Do NOT change the jobId format structure.
  </action>
  <verify>
    grep -n "substr(" platform/backend/src/api/routes/tickets.ts returns no results
    grep -n "randomUUID" platform/backend/src/api/routes/tickets.ts shows the new usage
  </verify>
  <done>
    - jobId generation uses crypto.randomUUID().slice(0, 8)
    - No substr() calls remain in tickets.ts
    - Code compiles without errors
  </done>
</task>

</tasks>

<verification>
1. grep -r "\.substr(" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist --exclude-dir=".next" returns no results from source files
2. All files compile with tsc --noEmit
3. Existing tests pass
</verification>

<success_criteria>
1. Zero substr() calls in source code
2. crypto.randomUUID() used for ID generation
3. All tests pass
4. No functional changes to ID format structure
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-application-organization-and-structural-refactoring/04.3-02-SUMMARY.md`
</output>
