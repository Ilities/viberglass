---
phase: 04.3-application-organization-and-structural-refactoring
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - platform/frontend/src/data.ts
autonomous: true

must_haves:
  truths:
    - "API errors are propagated to callers (not swallowed)"
    - "No mock data fallbacks in data.ts"
    - "Formatting utilities are properly organized"
    - "Error handling allows UI to display proper error states"
  artifacts:
    - path: platform/frontend/src/data.ts
      provides: "Clean data access layer without mock fallbacks"
      contains: "export async function"
      not_contains: "getMockTickets"
  key_links:
    - from: "platform/frontend/src/data.ts"
      to: "platform/frontend/src/service/api/*.ts"
      via: "API client imports"
      pattern: "from '@/service/api/"
---

<objective>
Remove mock data fallbacks from frontend data.ts and reorganize formatting utilities.

Purpose: Eliminate silent error swallowing that masks real API issues, improve error visibility for proper UI error handling.
Output: Clean data access layer that propagates errors properly and separates formatting utilities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/04.3-application-organization-and-structural-refactoring/04.3-RESEARCH.md

@platform/frontend/src/data.ts
@platform/frontend/src/service/api/ticket-api.ts
@platform/frontend/src/service/api/project-api.ts
@platform/frontend/src/service/api/clanker-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove mock fallbacks from data access functions</name>
  <files>platform/frontend/src/data.ts</files>
  <action>
    For each data access function, remove the try/catch that returns mock data:

    1. getProjectsList() - Remove try/catch, let errors propagate
    2. getProjectBySlug() - Remove try/catch, let errors propagate
    3. getRecentTickets() - Remove try/catch and getMockTickets() call, let errors propagate
    4. getTicketDetails() - Remove try/catch, let errors propagate
    5. getTicketStats() - This function currently only returns mock data. Either:
       a. Remove it entirely (no actual implementation), OR
       b. Add TODO comment indicating it needs a real API endpoint
       Recommend option (a) - remove the function entirely
    6. triggerAutoFix() - Remove try/catch silent handling, let errors propagate
    7. getClankersList() - Remove try/catch, let errors propagate
    8. getClankerBySlug() - Remove try/catch, let errors propagate
    9. getDeploymentStrategiesList() - Remove try/catch, let errors propagate

    Pattern to change FROM:
    ```typescript
    export async function getProjectsList(): Promise<Project[]> {
      try {
        return await apiGetProjects()
      } catch (error) {
        console.warn('Failed to fetch projects:', error)
        return []
      }
    }
    ```

    Pattern to change TO:
    ```typescript
    export async function getProjectsList(): Promise<Project[]> {
      return await apiGetProjects()
    }
    ```

    Rationale: The UI components using these functions should handle errors with proper loading/error states. Silently returning empty arrays or mock data hides real problems.
  </action>
  <verify>
    grep -n "console.warn" platform/frontend/src/data.ts returns no results
    grep -n "getMockTickets" platform/frontend/src/data.ts returns no results
    grep -n "return \[\]" platform/frontend/src/data.ts returns no results (silent empty returns)
    grep -n "return null" platform/frontend/src/data.ts returns no results (silent null returns)
  </verify>
  <done>
    - All data access functions propagate errors
    - No mock fallbacks remain
    - No silent error swallowing
    - Functions are simple one-liners that call API clients
  </done>
</task>

<task type="auto">
  <name>Task 2: Move formatting utilities to separate file</name>
  <files>platform/frontend/src/lib/formatters.ts</files>
  <action>
    Create a new file platform/frontend/src/lib/formatters.ts and move all formatting utility functions:

    1. Create platform/frontend/src/lib/ directory if it doesn't exist
    2. Create formatters.ts with the following functions moved from data.ts:
       - formatSeverity()
       - formatAutoFixStatus()
       - formatTicketSystem()
       - formatTimestamp()
       - formatClankerStatus()
       - formatDeploymentStrategy()

    3. Add proper re-exports in data.ts:
       ```typescript
       export * from './lib/formatters'
       ```

    4. Keep the type exports at the bottom of data.ts

    This separates concerns: data.ts becomes pure data access, formatters.ts becomes pure formatting utilities.
  </action>
  <verify>
    File platform/frontend/src/lib/formatters.ts exists with 6 formatting functions
    grep "export format" platform/frontend/src/lib/formatters.ts shows 6 exports
    data.ts imports from formatters.ts
  </verify>
  <done>
    - formatters.ts created with all formatting functions
    - data.ts re-exports from formatters.ts for backward compatibility
    - No functionality changes for consumers
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove unused mock data functions</name>
  <files>platform/frontend/src/data.ts</files>
  <action>
    Remove the following functions that are no longer used after removing mock fallbacks:
    - getMockTickets()
    - getMockTicketStats()

    These are internal functions that were only called from the now-refactored data access functions.
  </action>
  <verify>
    grep -n "getMockTickets" platform/frontend/src/data.ts returns no results
    grep -n "getMockTicketStats" platform/frontend/src/data.ts returns no results
  </verify>
  <done>
    - Mock data functions removed
    - File is cleaner and focused on real data access
  </done>
</task>

</tasks>

<verification>
1. npm run build completes without errors in frontend
2. No mock data fallbacks remain
3. All formatting functions work the same (backward compatible via re-exports)
4. Code is properly separated between data access and formatting
</verification>

<success_criteria>
1. data.ts is <100 lines (down from 271)
2. No console.warn calls in data.ts
3. All mock data functions removed
4. Formatting utilities properly separated
5. Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-application-organization-and-structural-refactoring/04.3-03-SUMMARY.md`
</output>
