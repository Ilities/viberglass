---
phase: 05-job-status-polling
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - platform/frontend/src/components/job-status-indicator.tsx
  - platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Job status indicator shows animated pulse when job is running and polling is active"
    - "Job detail page automatically refreshes job status every 3 seconds"
    - "User sees 'Job completed successfully' toast when job finishes"
    - "User sees 'Job failed' toast when job fails"
    - "Manual refresh button still works (router.refresh)"
    - "'Status may be outdated' message is removed (no longer needed)"
  artifacts:
    - path: "platform/frontend/src/components/job-status-indicator.tsx"
      provides: "Animated job status indicator with icon"
      exports: ["JobStatusIndicator"]
    - path: "platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx"
      provides: "Client component with automatic polling"
      contains: "'use client'"
  key_links:
    - from: "job-status-indicator.tsx"
      to: "badge.tsx"
      via: "Badge component for status styling"
      pattern: "import.*Badge.*from.*@/components/badge"
    - from: "page.tsx"
      to: "useJobStatus"
      via: "Import for automatic job status polling"
      pattern: "import.*useJobStatus.*from.*@/hooks/useJobStatus"
    - from: "page.tsx"
      to: "job-status-indicator.tsx"
      via: "Import for animated status display"
      pattern: "import.*JobStatusIndicator.*from.*@/components/job-status-indicator"
---

<objective>
Create animated JobStatusIndicator component and integrate polling into job detail page for automatic status updates.

Purpose: Provide visual feedback for job status with animated indicators, and convert the job detail page from server component to client component with automatic polling. Users will see status update in real-time without manual refresh.

Output: Job status polling fully working on job detail page with visual indicators and toast notifications.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-job-status-polling/05-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Existing components to modify
@platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx
@platform/frontend/src/components/badge.tsx
@platform/frontend/package.json

# Dependencies from prior plans
@platform/frontend/src/hooks/useJobStatus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JobStatusIndicator component</name>
  <files>platform/frontend/src/components/job-status-indicator.tsx</files>
  <action>
Create JobStatusIndicator component with animated status display:

1. Create `job-status-indicator.tsx` with:
   - Import Badge from '@/components/badge'
   - Import ClockIcon, CheckCircleIcon, XCircleIcon from '@heroicons/react/20/solid'
   - Import motion from 'motion'
   - Interface: JobStatusIndicatorProps { status: 'queued' | 'active' | 'completed' | 'failed', isPolling?: boolean }

2. Status config object with:
   - queued: { label: 'Queued', color: 'yellow', icon: ClockIcon }
   - active: { label: 'Running', color: 'blue', icon: ClockIcon }
   - completed: { label: 'Completed', color: 'green', icon: CheckCircleIcon }
   - failed: { label: 'Failed', color: 'red', icon: XCircleIcon }

3. Render:
   - Badge with appropriate color
   - Icon from config (ClockIcon, CheckCircleIcon, or XCircleIcon)
   - Animated pulse using motion when status is 'active' AND isPolling is true:
     - Use motion.div with animate={{ opacity: [1, 0.5, 1] }}
     - transition={{ duration: 1.5, repeat: Infinity }}
   - Optional: Ping animation for extra visual feedback (animate-ping class on a span)

Do NOT use animation for terminal states (completed/failed) - only animate when actively running.
  </action>
  <verify>
File exists at platform/frontend/src/components/job-status-indicator.tsx with:
- JobStatusIndicator component exported
- Uses Badge for base styling
- Animated pulse for active + isPolling state
- Icons: ClockIcon (queued/active), CheckCircleIcon (completed), XCircleIcon (failed)
  </verify>
  <done>
JobStatusIndicator displays job status with appropriate color, icon, and animated pulse for running jobs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert job detail page to client component with polling</name>
  <files>platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx</files>
  <action>
Convert job detail page from server component to client component with automatic polling:

1. Add 'use client' directive at top of file

2. Remove async/await from component (now a client component):
   - Change: `export default async function JobDetailPage({ params }: ...)`
   - To: `export default function JobDetailPage({ jobId, project }: { jobId: string, project: string })`
   - params are now passed directly (not awaited)

3. Import useJobStatus:
   - Add: `import { useJobStatus } from '@/hooks/useJobStatus'`
   - Add: `import { JobStatusIndicator } from '@/components/job-status-indicator'`

4. Call useJobStatus hook at start of component:
   - `const { job, isLoading, error, isPolling } = useJobStatus(jobId)`

5. Replace server-side getJob call with hook data:
   - Remove the try/catch getJob call
   - Handle loading state: `if (isLoading) return <div>Loading...</div>`
   - Handle error state: `if (error || !job) return <div>Job not found</div>`

6. Replace static Badge with JobStatusIndicator:
   - Find: `<Badge className={status.color}>{status.label}</Badge>`
   - Replace with: `<JobStatusIndicator status={job.status} isPolling={isPolling} />`

7. Remove 'Status may be outdated' message (lines 71-74):
   - The warning about manual refresh is no longer needed
   - Delete the entire conditional block that shows ClockIcon with "Status may be outdated - refresh for latest"

8. Keep JobRefreshButton for manual refresh (still useful)

9. Ensure all existing functionality remains:
   - PR link button (if job.result?.pullRequestUrl exists)
   - Task description display
   - Results section (success/changed files)
   - Error message display
   - Progress display for active jobs
   - Job details table
   - Duration formatting still works

Do NOT break existing display logic. Keep all the result/progress/details sections intact - just change the data source from server props to hook.
  </action>
  <verify>
File has 'use client' directive at top
Imports useJobStatus and JobStatusIndicator
Uses hook data instead of server-side getJob
Has loading/error state handling
JobStatusIndicator replaces static Badge
'Status may be outdated' message removed
All existing sections (task, results, details) still present
  </verify>
  <done>
Job detail page is now a client component that automatically polls and displays job status updates.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete job status polling with animated indicator and automatic refresh</what-built>
  <how-to-verify>
1. Start frontend: cd platform/frontend && npm run dev
2. Start backend: cd platform/backend && npm run dev
3. Navigate to http://localhost:3001
4. Create a ticket (or use existing one)
5. Run the ticket with a clanker
6. Navigate to the job detail page
7. Observe:
   - Status badge shows animated pulse when job is running
   - Status updates automatically every 3 seconds (no manual refresh)
   - When job completes, green toast appears with "Job completed successfully"
   - If job fails, red toast appears with error message
   - If PR was created, toast has "View PR" button
8. Test tab hiding: Switch to another tab, wait 10 seconds, return - polling should resume immediately
9. Verify manual refresh button still works
  </how-to-verify>
  <resume-signal>Type "approved" if polling works correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. JobStatusIndicator has proper TypeScript types
2. Page component is client-side ('use client')
3. Loading and error states display properly
4. Animated pulse only shows for active + isPolling
5. All original functionality preserved (PR links, error messages, details table)
6. Manual refresh button still functional
</verification>

<success_criteria>
- Job status updates automatically without user action
- Visual feedback shows polling is active (animated pulse)
- Toast notifications appear on job completion/failure
- Polling pauses when tab is hidden
- Polling stops when job reaches terminal state
- Manual refresh still available as backup
</success_criteria>

<output>
After completion, create `.planning/phases/05-job-status-polling/05-03-SUMMARY.md`
</output>
