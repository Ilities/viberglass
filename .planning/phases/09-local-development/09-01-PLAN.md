---
phase: 09-local-development
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/backend/Dockerfile.dev
  - platform/frontend/Dockerfile.dev
  - platform/backend/.dockerignore
  - platform/frontend/.dockerignore
autonomous: true

must_haves:
  truths:
    - "Backend container can be built and run locally"
    - "Frontend container can be built and run locally"
    - "Hot-reload works via nodemon (backend) and Next.js HMR (frontend)"
    - "node_modules preserved inside containers (not overwritten by host mount)"
    - "Both containers expose correct ports (8888 for backend, 3000 for frontend)"
  artifacts:
    - path: "platform/backend/Dockerfile.dev"
      provides: "Backend development container with hot-reload"
      contains: "FROM node:20-alpine", "nodemon", "EXPOSE 8888"
      min_lines: 20
    - path: "platform/frontend/Dockerfile.dev"
      provides: "Frontend development container with Next.js HMR"
      contains: "FROM node:20-alpine", "next dev", "EXPOSE 3000"
      min_lines: 20
    - path: "platform/backend/.dockerignore"
      provides: "Exclude unnecessary files from backend build"
      contains: "node_modules", "dist"
    - path: "platform/frontend/.dockerignore"
      provides: "Exclude unnecessary files from frontend build"
      contains: "node_modules", ".next"
  key_links:
    - from: "docker-compose.yml"
      to: "platform/backend/Dockerfile.dev"
      via: "build context and dockerfile reference"
      pattern: "context: ./platform/backend.*dockerfile: Dockerfile.dev"
    - from: "docker-compose.yml"
      to: "platform/frontend/Dockerfile.dev"
      via: "build context and dockerfile reference"
      pattern: "context: ./platform/frontend.*dockerfile: Dockerfile.dev"
---

<objective>
Create development Dockerfiles for both backend and frontend services with hot-reload support.

Purpose: Enable containerized local development where code changes trigger automatic reload without rebuilding images.

Output: Backend and frontend Dockerfile.dev files that support development workflow with mounted source volumes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-local-development/09-RESEARCH.md
@platform/backend/package.json
@platform/frontend/package.json
@viberator/infrastructure/docker/viberator-docker-worker.Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Backend Dockerfile.dev</name>
  <files>platform/backend/Dockerfile.dev</files>
  <action>
    Create platform/backend/Dockerfile.dev with:
    - Base image: node:20-alpine (matches Node.js 20 requirement from package.json engines)
    - Workdir: /app
    - Install ALL dependencies (including devDependencies like nodemon, tsx)
    - Copy source code
    - Expose port 8888 (backend API port)
    - Use nodemon for hot-reload: CMD ["npm", "run", "dev"]
    - Note: Volume mounting will be handled in docker-compose, not here

    Include .dockerignore to exclude:
    - node_modules/
    - dist/
    - .git/
    - *.log
    - .env.local

    Do NOT create production Dockerfile (that's out of scope for this phase)
  </action>
  <verify>
    docker build -f platform/backend/Dockerfile.dev -t viberator-backend:dev ./platform/backend && docker images | grep viberator-backend:dev
  </verify>
  <done>
    Backend Dockerfile.dev exists and builds successfully with node:20-alpine, installs all dependencies, exposes port 8888, and uses "npm run dev" as CMD
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Frontend Dockerfile.dev</name>
  <files>platform/frontend/Dockerfile.dev</files>
  <action>
    Create platform/frontend/Dockerfile.dev with:
    - Base image: node:20-alpine (matches Node.js requirement)
    - Workdir: /app
    - Install ALL dependencies (including devDependencies)
    - Copy source code
    - Expose port 3000 (Next.js default port)
    - Use Next.js dev server for HMR: CMD ["npm", "run", "dev"]
    - Note: Volume mounting will be handled in docker-compose, not here

    Include .dockerignore to exclude:
    - node_modules/
    - .next/
    - .git/
    - *.log
    - .env.local

    Do NOT create production Dockerfile (that's out of scope for this phase)
  </action>
  <verify>
    docker build -f platform/frontend/Dockerfile.dev -t viberator-frontend:dev ./platform/frontend && docker images | grep viberator-frontend:dev
  </verify>
  <done>
    Frontend Dockerfile.dev exists and builds successfully with node:20-alpine, installs all dependencies, exposes port 3000, and uses "npm run dev" as CMD
  </done>
</task>

<task type="auto">
  <name>Task 3: Create .dockerignore files</name>
  <files>platform/backend/.dockerignore, platform/frontend/.dockerignore</files>
  <action>
    Create .dockerignore for both backend and frontend to speed up builds and prevent issues:

    platform/backend/.dockerignore:
    ```
    node_modules
    dist
    npm-debug.log
    .git
    .env
    .env.local
    .env.*.local
    *.log
    coverage
    .idea
    .vscode
    ```

    platform/frontend/.dockerignore:
    ```
    node_modules
    .next
    out
    npm-debug.log
    .git
    .env
    .env.local
    .env.*.local
    *.log
    coverage
    .idea
    .vscode
    ```
  </action>
  <verify>test -f platform/backend/.dockerignore && test -f platform/frontend/.dockerignore && cat platform/backend/.dockerignore | grep node_modules</verify>
  <done>
    Both .dockerignore files exist and exclude node_modules and build artifacts
  </done>
</task>

</tasks>

<verification>
- Both Dockerfile.dev files build successfully: `docker build -f platform/backend/Dockerfile.dev -t viberator-backend:dev ./platform/backend`
- Frontend builds: `docker build -f platform/frontend/Dockerfile.dev -t viberator-frontend:dev ./platform/frontend`
- Images are tagged correctly and visible in `docker images`
- .dockerignore files prevent node_modules from being copied into build context
</verification>

<success_criteria>
1. Backend Dockerfile.dev produces ~1GB image (Node.js + dependencies)
2. Frontend Dockerfile.dev produces ~800MB image (Node.js + Next.js dependencies)
3. Both images can be built from repository root
4. .dockerignore files reduce build context size by excluding node_modules
5. Images use `npm run dev` which enables hot-reload when volumes are mounted
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-development/09-01-SUMMARY.md`
</output>
