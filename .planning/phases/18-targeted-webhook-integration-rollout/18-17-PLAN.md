# 18-17 Plan: Cleanup, Deprecation Removal, and Documentation

**Phase:** 18-targeted-webhook-integration-rollout  
**Plan:** 17  
**PR:** PR-17  
**Estimate:** 1 day  
**Status:** Planned  
**Depends on:** 18-01, 18-02, 18-03, 18-04, 18-05, 18-06, 18-07, 18-08, 18-09, 18-10, 18-11, 18-12, 18-13, 18-14, 18-15, 18-16

## Objective

Finalize phase by removing deprecated generic webhook paths and updating docs/tests to the provider-targeted model.

## Scope

In scope:
- remove or hard-deprecate legacy generic webhook API call paths
- update docs and testing plan to match final architecture
- verify no active callers remain on deprecated paths

Out of scope:
- net-new integration features

## Implementation Tasks

1. Remove deprecated frontend webhook API usage patterns.
2. Remove obsolete backend webhook endpoints or return explicit deprecation errors.
3. Update docs:
   - `TESTING_PLAN.md`
   - integration UX and implementation docs
   - phase summary docs as needed
4. Update e2e matrix to include per-provider targeted config flows.
5. Final regression pass on build/test commands.

## Candidate Files

- `apps/platform-frontend/src/service/api/webhook-api.ts`
- `apps/platform-backend/src/api/routes/projects.ts` (legacy webhook integration paths)
- `docs/*`
- `TESTING_PLAN.md`
- `tests/e2e/tests/integrations/*`

## Test Plan

1. Confirm no UI path calls removed endpoints.
2. Run backend and frontend test suites applicable to changed areas.
3. Run e2e integration settings coverage for all target providers.
4. Validate deprecation behavior for stale endpoints if retained temporarily.

## Risks and Mitigations

- Risk: hidden callers still use legacy contracts.  
  Mitigation: temporary deprecation responses + log warnings before hard removal.
- Risk: docs drift after refactor.  
  Mitigation: update docs in same PR with explicit endpoint and UI references.

## Acceptance Criteria

1. Deprecated generic webhook code paths are removed or explicitly deprecated.
2. Documentation reflects integration-targeted inbound/outbound architecture.
3. Tests cover final provider-specific behavior and pass.
4. Phase 18 is ready for execution tracking and summaries.

