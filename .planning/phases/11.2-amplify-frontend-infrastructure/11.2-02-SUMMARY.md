---
phase: 11.2-amplify-frontend-infrastructure
plan: 02
subsystem: infra
tags: amplify, oidc, github-actions, pulumi

# Dependency graph
requires:
  - phase: 11.2-amplify-frontend-infrastructure
    plan: 01
    provides: Amplify OIDC and frontend component modules
provides:
  - Amplify OIDC provider for GitHub Actions deployment
  - Amplify frontend app with SSR configuration
  - SSM parameters for Amplify app configuration
  - Pulumi stack exports for Amplify resources
affects: github-actions-deployment, ci-cd

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pulumi component integration pattern: import, instantiate, export"
    - "Environment-specific branch naming (production for prod, main otherwise)"
    - "Backend URL interpolation for environment variable injection"

key-files:
  created: []
  modified:
    - infrastructure/index.ts

key-decisions:
  - "Direct loadBalancer.albDnsName interpolation instead of backendUrl export reference - avoids forward reference issue in TypeScript"

patterns-established:
  - "Amplify component instantiation follows create-call-export pattern"
  - "Environment-aware branch configuration based on config.environment"

# Metrics
duration: 1min
completed: 2026-01-23
---

# Phase 11.2 Plan 02: Integrate Amplify Components into Pulumi Stack Summary

**Amplify OIDC provider and frontend app wired into main Pulumi infrastructure stack with proper backend URL passing and outputs exported**

## Performance

- **Duration:** 1 min
- **Started:** 2026-01-23T09:06:21Z
- **Completed:** 2026-01-23T09:07:47Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Amplify OIDC and frontend components imported into main infrastructure stack
- OIDC provider created for GitHub Actions Amplify deployment
- Amplify frontend app configured with backend URL from load balancer
- Amplify outputs exported for CI/CD reference (appId, appArn, domain, SSM paths)

## Task Commits

Each task was committed atomically:

1. **Task 1: Integrate Amplify components into main Pulumi stack** - `1fa52e8` (feat)

**Plan metadata:** None

## Files Created/Modified

- `infrastructure/index.ts` - Added Amplify component imports, instantiation, and exports

## Decisions Made

- **Direct interpolation for backendUrl**: Used `pulumi.interpolate`http://${loadBalancer.albDnsName}`` directly instead of referencing the later-declared `backendUrl` export to avoid TypeScript forward reference error.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

**TypeScript forward reference error**: Initial implementation used `backendUrl` in the Amplify component creation, but `backendUrl` is declared as an export later in the file. Fixed by using the same `pulumi.interpolate`http://${loadBalancer.albDnsName}`` expression directly in the component call.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Pulumi stack ready to provision Amplify resources
- GitHub Actions OIDC role available for Amplify deployment
- SSM parameters will be available for CI/CD workflows
- Phase 12 (Secret Management) can proceed

---
*Phase: 11.2-amplify-frontend-infrastructure*
*Completed: 2026-01-23*
