---
phase: 11.2-amplify-frontend-infrastructure
plan: 01
subsystem: infra
tags: [pulumi, aws, amplify, oidc, github-actions, iam, ssr, nextjs]

# Dependency graph
requires:
  - phase: 11
    provides: Amplify deployment configuration (amplify.yml, GitHub Actions workflows)
provides:
  - Pulumi component for GitHub Actions OIDC authentication with Amplify deployment permissions
  - Pulumi component for Amplify app and branch provisioning with WEB_COMPUTE platform for SSR
  - SSM Parameter Store integration for Amplify configuration (app ID, branch name, region)
affects: [12-secret-management, future-phases]

# Tech tracking
tech-stack:
  added: [@pulumi/aws IAM OpenIdConnectProvider, @pulumi/aws Amplify App and Branch, @pulumi/aws SSM Parameter]
  patterns: [Separate OIDC providers per concern (Pulumi vs Amplify), WEB_COMPUTE platform for SSR, SSM for CI/CD config storage]

key-files:
  created: [infrastructure/components/amplify-oidc.ts, infrastructure/components/amplify-frontend.ts]
  modified: []

key-decisions:
  - "Separate OIDC provider for Amplify deployment (not reuse Pulumi OIDC) maintains separation of concerns"
  - "WEB_COMPUTE platform required for SSR support (not WEB platform)"
  - "Auto-branch creation disabled for security"
  - "Build via GitHub Actions with enableAutoBuild: false"

patterns-established:
  - "Component pattern: Options interface → Outputs interface → Factory function"
  - "Resource naming: ${environment}-viberator-{resource}"
  - "SSM parameter paths: /viberator/{environment}/{service}/{key}"
  - "YAML buildSpec as template literal for Amplify apps"

# Metrics
duration: 4min
completed: 2026-01-23
---

# Phase 11.2 Plan 01: Amplify Frontend Infrastructure Summary

**GitHub Actions OIDC authentication and Amplify SSR app provisioning with WEB_COMPUTE platform and SSM configuration storage**

## Performance

- **Duration:** 4min (237 seconds)
- **Started:** 2026-01-23T08:58:51Z
- **Completed:** 2026-01-23T09:02:44Z
- **Tasks:** 2
- **Files created:** 2

## Accomplishments

- Created GitHub Actions OIDC provider and IAM role for Amplify deployment with least-privilege permissions
- Created Amplify app component with WEB_COMPUTE platform for SSR support (critical requirement)
- Configured Amplify branch with environment-specific settings (production/main, PRODUCTION/DEVELOPMENT)
- Stored Amplify configuration in SSM Parameter Store for CI/CD access

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Amplify OIDC component for GitHub Actions authentication** - `5d31106` (feat)
2. **Task 2: Create Amplify frontend component for app and branch provisioning** - `286827b` (feat)
3. **Bug fixes: TypeScript compilation errors** - `6c5d4b7` (fix)

**Plan metadata:** (to be committed)

## Files Created/Modified

- `infrastructure/components/amplify-oidc.ts` - GitHub Actions OIDC provider and IAM role for Amplify deployment
- `infrastructure/components/amplify-frontend.ts` - Amplify app and branch provisioning with SSR support

## Decisions Made

- **Separate OIDC provider for Amplify**: Created distinct OpenID Connect provider and IAM role for Amplify deployment (not reuse existing Pulumi OIDC) to maintain separation of concerns between infrastructure provisioning and application deployment
- **WEB_COMPUTE platform**: Used `platform: "WEB_COMPUTE"` instead of `WEB` to enable Next.js SSR support with Lambda@Edge
- **Auto-branch creation disabled**: Set `enableAutoBranchCreation: false` for security - only explicitly configured branches can be deployed
- **GitHub Actions-driven builds**: Set `enableAutoBuild: false` to build via GitHub Actions workflows instead of Amplify's auto-build

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed TypeScript property names in OIDC provider**
- **Found during:** Task 1 verification (TypeScript compilation)
- **Issue:** Plan specified `clientIdList` and `thumbprintList` but Pulumi AWS provider API uses `clientIdLists` and `thumbprintLists` (plural)
- **Fix:** Changed to `clientIdLists: ["sts.amazonaws.com"]` and `thumbprintLists: ["6938fd4d98bab03faadb97b34396831e3780aea1"]`
- **Files modified:** infrastructure/components/amplify-oidc.ts
- **Verification:** TypeScript compilation passes with `npx tsc --noEmit`
- **Committed in:** 6c5d4b7 (bug fix commit)

**2. [Rule 1 - Bug] Fixed buildSpec format in Amplify app**
- **Found during:** Task 2 verification (TypeScript compilation)
- **Issue:** Plan specified buildSpec as object literal but Pulumi AWS provider API requires YAML string
- **Fix:** Changed buildSpec from JavaScript object to YAML template literal with proper indentation
- **Files modified:** infrastructure/components/amplify-frontend.ts
- **Verification:** TypeScript compilation passes, buildSpec matches Amplify YAML format
- **Committed in:** 6c5d4b7 (bug fix commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - Bug fixes for API compatibility)
**Impact on plan:** Both auto-fixes necessary for TypeScript compilation and Pulumi AWS provider compatibility. No scope creep, just API corrections.

## Issues Encountered

- **TypeScript compilation errors**: Initial implementation used incorrect property names for IAM OpenIdConnectProvider and incorrect type for Amplify buildSpec. Fixed by consulting Pulumi AWS provider API and correcting to `clientIdLists`, `thumbprintLists` (plural) and YAML string format for buildSpec.

## User Setup Required

None - no external service configuration required. Components are ready for Pulumi deployment.

## Next Phase Readiness

- Amplify infrastructure components complete and ready for integration into Pulumi stack
- GitHub Actions deployment workflows already configured (from Phase 11)
- SSM parameters available for CI/CD configuration injection
- Ready for Phase 12: Secret Management

---
*Phase: 11.2-amplify-frontend-infrastructure*
*Completed: 2026-01-23*
