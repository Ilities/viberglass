---
phase: 10-aws-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/Pulumi.yaml
  - infrastructure/tsconfig.json
  - infrastructure/package.json
  - infrastructure/index.ts
autonomous: true
must_haves:
  truths:
    - "Pulumi infrastructure code exists in proper location"
    - "Pulumi stack can be selected (dev/staging/prod)"
    - "Dependencies are installable and TypeScript compiles"
  artifacts:
    - path: "infrastructure/Pulumi.yaml"
      provides: "Pulumi project configuration with stack support"
      contains: "stack"
    - path: "infrastructure/package.json"
      provides: "Dependencies for Pulumi infrastructure"
      contains: "@pulumi/pulumi"
    - path: "infrastructure/tsconfig.json"
      provides: "TypeScript configuration for infrastructure"
      contains: "commonjs"
    - path: "infrastructure/index.ts"
      provides: "Main infrastructure entry point"
      min_lines: 50
  key_links:
    - from: "infrastructure/Pulumi.yaml"
      to: "pulumi stack select"
      via: "pulumi CLI"
      pattern: "pulumi stack select"
---

<objective>
Reorganize Pulumi infrastructure to proper location with multi-stack support (dev/staging/prod).

Purpose: The existing infrastructure code lives at `viberator/infrastructure/infra/` which is nested awkwardly. This plan moves it to a top-level `infrastructure/` directory and adds proper Pulumi configuration for multiple stacks (dev, staging, prod).

Output: Reorganized Pulumi project with stack configuration, installable dependencies, and compilable TypeScript.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/jussi/Development/viberator/viberator/infrastructure/infra/index.ts
@/home/jussi/Development/viberator/viberator/infrastructure/infra/Pulumi.yaml
@/home/jussi/Development/viberator/viberator/infrastructure/infra/package.json
@/home/jussi/Development/viberator/viberator/infrastructure/infra/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infrastructure directory structure and move Pulumi configuration</name>
  <files>infrastructure/Pulumi.yaml, infrastructure/package.json, infrastructure/tsconfig.json</files>
  <action>
Create top-level `infrastructure/` directory structure:

1. Create `infrastructure/Pulumi.yaml` with multi-stack configuration:
   - Set name to `viberator`
   - Configure runtime as nodejs
   - Add template for dev, staging, prod stacks

2. Create `infrastructure/package.json`:
   - Copy dependencies from `viberator/infrastructure/infra/package.json`
   - Add name: `@viberator/infrastructure`
   - Add scripts: "pulumi": "pulumi", "preview": "pulumi preview", "up": "pulumi up -y"

3. Create `infrastructure/tsconfig.json`:
   - Copy from existing but update paths
   - Set outDir to "bin"
   - Include all .ts files in directory

4. Create `.gitignore` in infrastructure/ for:
   - node_modules/
   - bin/
   - .pulumi/
   - Pulumi.*.yaml

Do NOT delete the old infrastructure yet - that will happen in next phase after migration is verified.
  </action>
  <verify>
    - `ls -la infrastructure/` shows Pulumi.yaml, package.json, tsconfig.json
    - `cd infrastructure && npm install` completes without errors
    - `pulumi stack ls` shows available stacks (dev, staging, prod)
  </verify>
  <done>
    Top-level infrastructure directory exists with Pulumi configuration. Three stacks (dev, staging, prod) are configured. Dependencies install successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate infrastructure code with modular structure</name>
  <files>infrastructure/index.ts, infrastructure/components/</files>
  <action>
Create modular infrastructure code structure:

1. Create `infrastructure/index.ts` as main entry point:
   - Import all components
   - Export stack outputs
   - Read stack configuration (AWS region, environment)

2. Create `infrastructure/components/registry.ts`:
   - Migrate ECR repository code from existing infra/index.ts
   - Export repository URL

3. Create `infrastructure/components/queue.ts`:
   - Migrate SQS queue code from existing infra/index.ts
   - Export queue URL and ARN

4. Create `infrastructure/components/worker-lambda.ts`:
   - Migrate Lambda function code from existing infra/index.ts
   - Keep IAM role with SSM access
   - Keep environment variables

5. Create `infrastructure/components/worker-ecs.ts`:
   - Migrate ECS cluster and task definition from existing infra/index.ts
   - Keep Container Insights enabled
   - Keep log configuration pointing to CloudWatch

Each component should:
- Accept a config object with region, environment, tags
- Export the created resources (not just inline)
- Use consistent naming: `${environment}-viberator-{resource}`

Keep the existing behavior - do not change infrastructure logic, only reorganize.
  </action>
  <verify>
    - `cd infrastructure && npx tsc` compiles without errors
    - `pulumi preview --stack dev` runs and shows planned resources
    - All components (registry, queue, worker-lambda, worker-ecs) are in separate files
  </verify>
  <done>
    Infrastructure code migrated to modular structure. TypeScript compiles. Pulumi preview shows all planned resources without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create stack configuration files</name>
  <files>Pulumi.dev.yaml, Pulumi.staging.yaml, Pulumi.prod.yaml</files>
  <action>
Create stack-specific configuration files:

1. Create `Pulumi.dev.yaml`:
   - Set aws:region to eu-west-1
   - Define dev-specific tags (Environment: dev)
   - Set cost-optimized settings (Fargate Spot enabled)

2. Create `Pulumi.staging.yaml`:
   - Set aws:region to eu-west-1
   - Define staging tags (Environment: staging)
   - Set medium capacity settings

3. Create `Pulumi.prod.yaml`:
   - Set aws:region to eu-west-1
   - Define production tags (Environment: production)
   - Set high-availability settings (no Spot, multi-AZ)

Add config keys:
- viberator:awsRegion
- viberator:environment
- viberator:enableSpot (boolean)
- viberator:containerInsights (boolean)

Read these configs in infrastructure/index.ts using `pulumi.getConfig()`.
  </action>
  <verify>
    - `pulumi config --stack dev` shows dev configuration
    - `pulumi config --stack prod` shows production configuration
    - `cat Pulumi.dev.yaml` contains aws:region and viberator:environment keys
  </verify>
  <done>
    Three stack configuration files exist. Each has appropriate region and environment settings. Config values are readable via Pulumi CLI.
  </done>
</task>

</tasks>

<verification>
1. `cd infrastructure && pulumi stack ls` lists dev, staging, prod stacks
2. `npm install && npx tsc` compiles successfully
3. `pulumi preview --stack dev` shows migrated resources without errors
4. All components are in separate files under components/
5. Old infrastructure code at viberator/infrastructure/ still exists (not deleted yet)
</verification>

<success_criteria>
- Pulumi infrastructure at `infrastructure/` with multi-stack support
- Modular component structure for maintainability
- TypeScript compiles without errors
- Three stacks configured: dev, staging, prod
- Pulumi preview succeeds for dev stack
</success_criteria>

<output>
After completion, create `.planning/phases/10-aws-infrastructure/10-01-SUMMARY.md`
</output>
