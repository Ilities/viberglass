---
phase: 07-clanker-runtime-status
plan: 04
type: execute
wave: 4
depends_on: ["07-02"]
files_modified:
  - platform/frontend/src/service/api/job-api.ts
  - platform/frontend/src/hooks/useJobStatus.ts
  - platform/frontend/src/components/progress-timeline.tsx
  - platform/frontend/src/components/log-viewer.tsx
  - platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Job detail page shows progress timeline with step history"
    - "Job detail page shows last seen timestamp with stale indicator"
    - "Log viewer displays job logs with color-coded levels"
    - "Progress updates are fetched via polling (existing useJobStatus pattern)"
  artifacts:
    - path: "platform/frontend/src/service/api/job-api.ts"
      provides: "Extended JobStatus type with progressUpdates and lastHeartbeat"
      exports: ["JobStatus", "ProgressUpdate", "LogEntry"]
    - path: "platform/frontend/src/components/progress-timeline.tsx"
      provides: "ProgressTimeline component"
      exports: ["ProgressTimeline"]
    - path: "platform/frontend/src/components/log-viewer.tsx"
      provides: "LogViewer component"
      exports: ["LogViewer"]
    - path: "platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx"
      provides: "Job detail page with progress and logs"
      contains: "ProgressTimeline"
  key_links:
    - from: "useJobStatus hook"
      to: "/api/jobs/:jobId"
      via: "fetch"
      pattern: "fetch.*api/jobs"
    - from: "ProgressTimeline"
      to: "job.progressUpdates"
      via: "props"
      pattern: "progressUpdates.*map"
---

<objective>
Create frontend UI for progress timeline and log viewer with last seen indicator.

Purpose: Users need visibility into worker execution progress and logs. The job detail page should show a timeline of progress updates, the last seen timestamp with stale indicator, and a log viewer with color-coded log levels.

Output: Extended types, ProgressTimeline component, LogViewer component, integrated job detail page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-clanker-runtime-status/07-CONTEXT.md
@.planning/phases/07-clanker-runtime-status/07-RESEARCH.md

@platform/frontend/src/service/api/job-api.ts
@platform/frontend/src/hooks/useJobStatus.ts
@platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx
@platform/frontend/src/components/job-status-indicator.tsx
@.planning/phases/07-clanker-runtime-status/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend types for progress and logs</name>
  <files>platform/frontend/src/service/api/job-api.ts</files>
  <action>
    Extend JobStatus interface and add new types:

    1. Add ProgressUpdate interface:
       ```typescript
       export interface ProgressUpdate {
         step: string | null
         message: string
         details: Record<string, unknown> | null
         createdAt: string
       }
       ```

    2. Add LogEntry interface:
       ```typescript
       export interface LogEntry {
         id: string
         level: 'info' | 'warn' | 'error' | 'debug'
         message: string
         source: string | null
         createdAt: string
       }
       ```

    3. Extend JobStatus interface with:
       - lastHeartbeat: string | null
       - progressUpdates: ProgressUpdate[]
       - logs: LogEntry[]

    Follow the existing JobStatus interface pattern.
  </action>
  <verify>
    Run `npm run build` in platform/frontend to verify type checking passes.
    Check that interfaces are exported.
  </verify>
  <done>
    TypeScript types compile without errors.
    New interfaces added and JobStatus extended.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProgressTimeline component</name>
  <files>platform/frontend/src/components/progress-timeline.tsx</files>
  <action>
    Create client component ProgressTimeline with:

    Props: { updates: ProgressUpdate[]; currentStatus: string }

    UI structure:
    - Subheading "Execution Progress"
    - Latest update (prominent): blue border, step name bold, message below, timestamp
    - Full history (timeline): vertical list with dots, each showing:
      - Step name (or "System" if no step)
      - Timestamp (formatted as HH:MM:SS)
      - Message (gray text)

    Styling:
    - Use Tailwind classes matching job detail page
    - Timeline dots: rounded-full bg-zinc-400, h-2 w-2
    - Latest update: border border-blue-200 bg-blue-50 dark:border-blue-500/30 dark:bg-blue-500/10
    - Use existing Subheading and Badge components from @/components

    Empty state: "No progress updates yet" in gray text.

    Follow the component pattern from job-status-indicator.tsx.
  </action>
  <verify>
    Check that component compiles without errors.
    Verify 'use client' directive at top of file.
  </verify>
  <done>
    ProgressTimeline component exists and displays updates.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create LogViewer component</name>
  <files>platform/frontend/src/components/log-viewer.tsx</files>
  <action>
    Create client component LogViewer with:

    Props: { logs: LogEntry[]; isConnected?: boolean }

    UI structure:
    - Header with Subheading "Logs" and live indicator (green dot when connected)
    - Log entries container: pre tag with monospace font, dark background, rounded, max-height 96 (scrollable)
    - Each log entry: timestamp, level badge, message
    - Color coding by level:
      - info: zinc/gray
      - warn: yellow/orange
      - error: red
      - debug: dimmed gray

    Styling:
    - Background: bg-zinc-900 text-zinc-100
    - Font: font-mono text-sm
    - Level badges: use Badge component with appropriate colors
    - Scrollable: overflow-auto max-h-96

    Empty state: "No logs available" in gray text.

    Follow the component pattern from existing components.
  </action>
  <verify>
    Check that component compiles without errors.
    Verify 'use client' directive at top of file.
  </verify>
  <done>
    LogViewer component exists and displays logs.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update useJobStatus to include progress and logs</name>
  <files>platform/frontend/src/hooks/useJobStatus.ts</files>
  <action>
    Extend useJobStatus hook to handle new fields:

    1. Update the return type to include lastHeartbeat, progressUpdates, logs
    2. The API already returns these (from backend), just need to ensure they're passed through

    If the backend doesn't yet return progressUpdates/logs, extend getJob() in job-api.ts to fetch them:
    - GET /api/jobs/:jobId returns: { progressUpdates, logs, lastHeartbeat }

    For now, assume backend extends getJobStatus() in JobService to include:
    - SELECT from job_progress_updates WHERE job_id = $1 ORDER BY created_at DESC
    - SELECT from job_log_lines WHERE job_id = $1 ORDER BY created_at DESC LIMIT 100

    Keep the existing polling behavior (3-second interval, stops on terminal state).
  </action>
  <verify>
    Check that hook compiles without errors.
    Verify polling still works for status.
  </verify>
  <done>
    useJobStatus returns progressUpdates, logs, lastHeartbeat.
  </done>
</task>

<task type="auto">
  <name>Task 5: Integrate ProgressTimeline and LogViewer into job detail page</name>
  <files>platform/frontend/src/app/(app)/project/[project]/jobs/[jobId]/page.tsx</files>
  <action>
    Update job detail page to include progress and logs:

    1. Import ProgressTimeline and LogViewer components
    2. Add "Last seen" row to Details table:
       - Label: "Last seen"
       - Value: job.lastHeartbeat ? formatted date : "Never"
       - Add stale indicator (yellow badge) if job.active and lastHeartbeat > 5 minutes ago
    3. Add ProgressTimeline section:
       - Place after Results section or in the grid (lg:col-span-2)
       - Pass job.progressUpdates and job.status
    4. Add LogViewer section:
       - Place after ProgressTimeline
       - Pass job.logs (or empty array if undefined)
       - Show isConnected={isPolling} for live indicator

    Follow the existing layout pattern:
    - Grid: lg:grid-cols-3
    - Use Subheading for section titles
    - Match styling from Results section

    Hide progress section if job.status is 'queued' (no progress yet).
  </action>
  <verify>
    Run `npm run build` to verify no errors.
    Check that all imports are present.
  </verify>
  <done>
    Job detail page shows progress timeline and log viewer.
    Last seen timestamp displays with stale indicator.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete progress timeline and log viewer UI integrated into job detail page</what-built>
  <how-to-verify>
    1. Start backend: cd platform/backend && npm run dev
    2. Start frontend: cd platform/frontend && npm run dev
    3. Create a job via the UI (run ticket)
    4. Navigate to the job detail page
    5. Verify:
       - Progress timeline shows execution steps
       - Last seen timestamp appears in Details table
       - Log viewer displays log lines (if any)
       - Stale indicator appears if job is active but no recent heartbeat
       - Styling matches existing job detail page
    6. Test with a completed job to verify full progress history
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- ProgressTimeline displays updates chronologically
- LogViewer shows color-coded log entries
- Last seen timestamp shows formatted date/time
- Stale indicator appears for long-running jobs
- Polling continues to update progress and logs
- UI matches existing job detail page styling
</verification>

<success_criteria>
Users can see job progress timeline on detail page.
Users can see execution logs with level colors.
Users can see last seen timestamp with stale indicator.
Progress updates refresh via polling.
</success_criteria>

<output>
After completion, create `.planning/phases/07-clanker-runtime-status/07-04-SUMMARY.md`
</output>
