---
phase: 07-clanker-runtime-status
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - platform/backend/src/api/routes/jobs.ts
  - platform/backend/src/services/JobService.ts
  - platform/backend/src/api/middleware/validation.ts
autonomous: true

must_haves:
  truths:
    - "Workers can POST progress updates to /api/jobs/:jobId/progress"
    - "Workers can POST log lines to /api/jobs/:jobId/logs"
    - "Progress endpoint validates tenant ownership (SEC-03)"
    - "Heartbeat timestamp is updated on each progress call"
  artifacts:
    - path: "platform/backend/src/api/routes/jobs.ts"
      provides: "POST /:jobId/progress and POST /:jobId/logs endpoints"
      exports: ["POST /:jobId/progress", "POST /:jobId/logs"]
    - path: "platform/backend/src/services/JobService.ts"
      provides: "recordProgress() and recordLog() methods"
      exports: ["recordProgress", "recordLog"]
  key_links:
    - from: "POST /:jobId/progress"
      to: "jobs.last_heartbeat"
      via: "JobService.recordProgress()"
      pattern: "updateTable.*last_heartbeat"
    - from: "POST /:jobId/logs"
      to: "job_log_lines table"
      via: "JobService.recordLog()"
      pattern: "insertInto.*job_log_lines"
---

<objective>
Create backend API endpoints for workers to send progress updates and log lines.

Purpose: Workers need to send progress updates (which double as heartbeats) and log lines during execution. Progress updates update the jobs table and store history. Log lines are stored for frontend display.

Output: Two new API endpoints with tenant validation, new JobService methods.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-clanker-runtime-status/07-CONTEXT.md
@.planning/phases/07-clanker-runtime-status/07-RESEARCH.md

@platform/backend/src/api/routes/jobs.ts
@platform/backend/src/services/JobService.ts
@platform/backend/src/api/middleware/validation.ts
@.planning/phases/07-clanker-runtime-status/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progress and log service methods to JobService</name>
  <files>platform/backend/src/services/JobService.ts</files>
  <action>
    Add two new methods to JobService class:

    1. async recordProgress(jobId: string, progress: { step?: string; message: string; details?: Record<string, unknown> }): Promise<void>
       - Update jobs.last_heartbeat = CURRENT_TIMESTAMP
       - Update jobs.progress = { step, message, details }
       - Insert into job_progress_updates (job_id, step, message, details, created_at)
       - Use transaction for atomic updates

    2. async recordLog(jobId: string, log: { level: 'info' | 'warn' | 'error' | 'debug'; message: string; source?: string }): Promise<void>
       - Insert into job_log_lines (job_id, level, message, source, created_at)
       - No update to jobs table (logs are separate)

    Follow the existing Kysely pattern from updateJobStatus() and submitJob().
    Use db.transaction().execute() for atomic operations in recordProgress().

    Console log format: [JobService] Job ${jobId} progress: ${message} / log: ${level}
  </action>
  <verify>
    Check that methods follow existing Kysely patterns.
    Verify transaction is used for atomic updates in recordProgress.
    </verify>
  <done>
    JobService.recordProgress() and JobService.recordLog() methods exist and compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create progress update validation schema</name>
  <files>platform/backend/src/api/middleware/validation.ts</files>
  <action>
    Add Joi validation schema for progress updates:

    export const validateProgressUpdate = [
      body('step').optional().isString().max(100),
      body('message').notEmpty().isString().max(1000),
      body('details').optional().isObject(),
      validateResult
    ];

    Add Joi validation schema for log entries:

    export const validateLogEntry = [
      body('level').isIn(['info', 'warn', 'error', 'debug']),
      body('message').notEmpty().isString().max(5000),
      body('source').optional().isString().max(100),
      validateResult
    ];

    Follow the exact pattern from validateResultCallback already in the file.
  </action>
  <verify>
    Check that validation schemas match existing patterns.
    Verify export statements are added.
  </verify>
  <done>
    Validation schemas exist and follow existing patterns.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add progress and log endpoints to jobs route</name>
  <files>platform/backend/src/api/routes/jobs.ts</files>
  <action>
    Add two new endpoints to jobs router (after POST /:jobId/result):

    1. POST /:jobId/progress
       - Middleware: tenantMiddleware, validateProgressUpdate
       - Verify job exists and belongs to tenant (like result endpoint)
       - Call jobService.recordProgress(jobId, { step, message, details })
       - Return { success: true, jobId }

    2. POST /:jobId/logs
       - Middleware: tenantMiddleware, validateLogEntry
       - Verify job exists and belongs to tenant
       - Call jobService.recordLog(jobId, { level, message, source })
       - Return { success: true }

    Follow the exact pattern from POST /:jobId/result including:
    - Tenant verification with 403 on mismatch
    - 404 if job not found
    - Try-catch with console.error
  </action>
  <verify>
    Check that endpoints follow the POST /:jobId/result pattern.
    Verify tenantMiddleware is applied.
    Run npm run build to verify no TypeScript errors.
  </verify>
  <done>
    Endpoints exist at /api/jobs/:jobId/progress and /api/jobs/:jobId/logs.
  </done>
</task>

</tasks>

<verification>
- Endpoints respond to POST requests
- Tenant validation works (SEC-03)
- Progress updates update jobs.last_heartbeat
- Log lines are stored in job_log_lines table
- 404 returned for non-existent jobs
- 403 returned for cross-tenant access
</verification>

<success_criteria>
Workers can send progress updates and log lines via API.
Progress updates update heartbeat timestamp.
Tenant isolation is enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-clanker-runtime-status/07-02-SUMMARY.md`
</output>
