---
phase: 01-multi-tenant-security-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - platform/backend/src/credentials/providers/FileProvider.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "FileProvider stores encrypted credentials in JSON file"
    - "AES-256-GCM encryption with 12-byte IV for each write"
    - "File operations are scoped by tenantId in nested structure"
    - "Encryption key is derived from CREDENTIALS_ENCRYPTION_KEY env var"
  artifacts:
    - path: "platform/backend/src/credentials/providers/FileProvider.ts"
      provides: "Encrypted file credential provider"
      exports: ["FileProvider"]
      min_lines: 120
  key_links:
    - from: "platform/backend/src/credentials/providers/FileProvider.ts"
      to: "platform/backend/src/credentials/CredentialProvider.ts"
      via: "implements CredentialProvider"
      pattern: "implements CredentialProvider"
    - from: "platform/backend/src/credentials/providers/FileProvider.ts"
      to: "Node.js crypto module"
      via: "import { createCipheriv, createDecipheriv, randomBytes }"
      pattern: "createCipheriv|createDecipheriv|randomBytes"
---

<objective>
Implement FileProvider for encrypted local file credential storage. This provider enables local development without AWS dependencies while maintaining security through AES-256-GCM encryption.

Purpose: Provide a development-friendly credential storage option that works locally without cloud dependencies, using industry-standard encryption to protect credentials at rest.

Output: FileProvider implementation with AES-256-GCM encryption, tenant-scoped storage structure, and proper key derivation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-tenant-security-foundation/01-CONTEXT.md
@.planning/phases/01-multi-tenant-security-foundation/01-RESEARCH.md

@viberator/app/src/scm/types.ts
@.planning/phases/01-multi-tenant-security-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement FileProvider with AES-256-GCM encryption</name>
  <files>platform/backend/src/credentials/providers/FileProvider.ts</files>
  <action>
Create FileProvider implementing CredentialProvider interface with encrypted file storage using AES-256-GCM encryption.

**File: platform/backend/src/credentials/providers/FileProvider.ts**
```typescript
import { CredentialProvider } from '../CredentialProvider';
import { createCipheriv, createDecipheriv, randomBytes, scrypt } from 'crypto';
import { promises as fs } from 'fs';
import { join } from 'path';

/**
 * File storage structure (in-memory, before encryption):
 * {
 *   "tenant-123": {
 *     "GITHUB_TOKEN": "ghp_...",
 *     "CLAUDE_API_KEY": "sk-ant-..."
 *   },
 *   "tenant-456": {
 *     "GITLAB_TOKEN": "glpat-..."
 *   }
 * }
 */
type CredentialStore = Record<string, Record<string, string>>;

/**
 * Encrypted file credential provider
 * Uses AES-256-GCM encryption for local development
 *
 * Security properties:
 * - AES-256-GCM provides authenticated encryption (AEAD)
 * - 12-byte IV for GCM mode (better interoperability)
 * - 16-byte auth tag automatically handled by Node crypto
 * - Scrypt key derivation from environment variable
 */
export class FileProvider implements CredentialProvider {
  readonly name = 'FileProvider';

  private static readonly ALGORITHM = 'aes-256-gcm';
  private static readonly IV_LENGTH = 12; // 12 bytes for GCM
  private static readonly AUTH_TAG_LENGTH = 16;
  private static readonly SCRYPT_KEY_LENGTH = 32; // 256 bits for AES-256
  private static readonly SCRYPT_SALT_LENGTH = 16;
  private static readonly SCRYPT_OPTIONS = {
    N: 32768,  // CPU cost parameter
    r: 8,      // Block size parameter
    p: 1,      // Parallelization parameter
  };

  private filePath: string;
  private encryptionKey: Buffer;
  private cache: CredentialStore | null = null;
  private cacheModified = false;

  constructor(config: { filePath?: string; encryptionKey?: string }) {
    this.filePath = config.filePath ||
      process.env.CREDENTIALS_FILE_PATH ||
      join(process.cwd(), '.credentials.json');

    const keySource = config.encryptionKey || process.env.CREDENTIALS_ENCRYPTION_KEY;

    if (!keySource) {
      throw new Error(
        'FileProvider requires CREDENTIALS_ENCRYPTION_KEY environment variable ' +
        '(64-character hex string for 32-byte key) or encryptionKey config option'
      );
    }

    // Derive 32-byte key using scrypt for better security than raw hex
    this.encryptionKey = this.deriveKey(keySource);
  }

  /**
   * Derive encryption key from source using scrypt
   * This is more secure than using raw hex as it provides key stretching
   */
  private deriveKey(source: string): Buffer {
    // If source is already 64 hex chars (32 bytes), use it directly
    if (/^[0-9a-f]{64}$/i.test(source)) {
      return Buffer.from(source, 'hex').slice(0, 32);
    }

    // Otherwise derive using scrypt (use source as password, fixed salt for reproducibility)
    const salt = Buffer.from('viberator-credentials-salt-v1', 'utf-8').slice(0, FileProvider.SCRYPT_SALT_LENGTH);
    return scrypt.sync(source, salt, FileProvider.SCRYPT_KEY_LENGTH);
  }

  async get(tenantId: string, key: string): Promise<string | null> {
    const store = await this.readStore();

    const tenantCreds = store[tenantId];
    if (!tenantCreds) {
      return null;
    }

    return tenantCreds[key] ?? null;
  }

  async put(tenantId: string, key: string, value: string): Promise<void> {
    const store = await this.readStore();

    if (!store[tenantId]) {
      store[tenantId] = {};
    }

    store[tenantId][key] = value;
    this.cacheModified = true;

    await this.writeStore(store);
  }

  async delete(tenantId: string, key: string): Promise<void> {
    const store = await this.readStore();

    if (!store[tenantId]) {
      return; // Nothing to delete
    }

    delete store[tenantId][key];

    // Clean up empty tenant entries
    if (Object.keys(store[tenantId]).length === 0) {
      delete store[tenantId];
    }

    this.cacheModified = true;
    await this.writeStore(store);
  }

  async isAvailable(): Promise<boolean> {
    try {
      // Check if we can read/write to the file location
      await this.readStore();
      return true;
    } catch {
      return false;
    }
  }

  async listKeys(tenantId: string): Promise<string[]> {
    const store = await this.readStore();
    const tenantCreds = store[tenantId];
    return tenantCreds ? Object.keys(tenantCreds) : [];
  }

  /**
   * Read and decrypt the credential store
   * Uses in-memory cache to avoid repeated decryption
   */
  private async readStore(): Promise<CredentialStore> {
    // Return cached version if available and not modified
    if (this.cache && !this.cacheModified) {
      return this.cache;
    }

    try {
      const encrypted = await fs.readFile(this.filePath);
      const decrypted = this.decrypt(encrypted);
      this.cache = decrypted;
      this.cacheModified = false;
      return decrypted;
    } catch (error: unknown) {
      if ((error as NodeJS.ErrnoException).code === 'ENOENT') {
        // File doesn't exist yet, return empty store
        this.cache = {};
        this.cacheModified = false;
        return {};
      }
      throw error;
    }
  }

  /**
   * Encrypt and write the credential store
   */
  private async writeStore(store: CredentialStore): Promise<void> {
    const encrypted = this.encrypt(store);
    await fs.writeFile(this.filePath, encrypted, { mode: 0o600 }); // Owner read/write only
    this.cache = store;
    this.cacheModified = false;
  }

  /**
   * Decrypt data from file
   * Format: [IV (12 bytes)][Auth Tag (16 bytes)][Ciphertext]
   */
  private decrypt(encrypted: Buffer): CredentialStore {
    if (encrypted.length < FileProvider.IV_LENGTH + FileProvider.AUTH_TAG_LENGTH) {
      throw new Error('Encrypted file too short to be valid');
    }

    const iv = encrypted.subarray(0, FileProvider.IV_LENGTH);
    const authTag = encrypted.subarray(
      FileProvider.IV_LENGTH,
      FileProvider.IV_LENGTH + FileProvider.AUTH_TAG_LENGTH
    );
    const ciphertext = encrypted.subarray(FileProvider.IV_LENGTH + FileProvider.AUTH_TAG_LENGTH);

    const decipher = createDecipheriv(FileProvider.ALGORITHM, this.encryptionKey, iv);
    decipher.setAuthTag(authTag);

    const decrypted = Buffer.concat([decipher.update(ciphertext), decipher.final()]);

    try {
      return JSON.parse(decrypted.toString('utf-8'));
    } catch (error) {
      throw new Error('Failed to parse decrypted credentials file (may be corrupted)');
    }
  }

  /**
   * Encrypt data for file storage
   * Format: [IV (12 bytes)][Auth Tag (16 bytes)][Ciphertext]
   */
  private encrypt(data: CredentialStore): Buffer {
    const plaintext = Buffer.from(JSON.stringify(data, null, 2), 'utf-8');
    const iv = randomBytes(FileProvider.IV_LENGTH);

    const cipher = createCipheriv(FileProvider.ALGORITHM, this.encryptionKey, iv);
    const ciphertext = Buffer.concat([cipher.update(plaintext), cipher.final()]);
    const authTag = cipher.getAuthTag();

    return Buffer.concat([iv, authTag, ciphertext]);
  }
}
```

Key implementation details:
1. Use scrypt for key derivation from CREDENTIALS_ENCRYPTION_KEY
2. 12-byte IV for GCM mode (better interoperability)
3. File permissions 0o600 (owner read/write only)
4. In-memory cache to avoid repeated decryption
5. Clean up empty tenant entries on delete
6. Proper error handling for missing files
  </action>
  <verify>
grep -r "class FileProvider implements CredentialProvider" platform/backend/src/credentials/providers/FileProvider.ts
grep -r "aes-256-gcm\|createCipheriv\|createDecipheriv" platform/backend/src/credentials/providers/FileProvider.ts
  </verify>
  <done>
FileProvider exists with:
- Implements CredentialProvider interface
- AES-256-GCM encryption with 12-byte IV
- Scrypt key derivation from CREDENTIALS_ENCRYPTION_KEY
- Tenant-scoped nested storage structure
- In-memory cache for performance
- File permissions 0o600 for security
- Proper error handling for missing/invalid files
  </done>
</task>

<task type="auto">
  <name>Add credentials file to gitignore</name>
  <files>.gitignore</files>
  <action>
Ensure the credentials file is in .gitignore to prevent committing encrypted credentials to version control.

Add to .gitignore if not already present:
```
# Encrypted local credentials (never commit)
.credentials.json
```

Check if the file exists first, and add only if missing.
  </action>
  <verify>
grep -q "\.credentials\.json" /home/jussi/Development/viberator/.gitignore
  </verify>
  <done>
.credentials.json is in .gitignore to prevent committing local credentials
  </done>
</task>

</tasks>

<verification>
1. FileProvider compiles without errors: `cd platform/backend && npx tsc --noEmit`
2. Encryption/decryption round-trip test passes (manual or unit test)
3. File has correct permissions (0o600) when created
4. Missing key configuration throws descriptive error
5. .credentials.json is in .gitignore
</verification>

<success_criteria>
- FileProvider implements CredentialProvider with full CRUD operations
- AES-256-GCM encryption with 12-byte IV for each write
- Scrypt key derivation from CREDENTIALS_ENCRYPTION_KEY
- Tenant-scoped storage structure
- In-memory caching for performance
- Proper error messages for missing keys/invalid files
- Credentials file excluded from git
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-security-foundation/01-02-SUMMARY.md` with:
- Encryption implementation details
- Key derivation approach
- File format specification
- Setup requirements for local development
</output>
