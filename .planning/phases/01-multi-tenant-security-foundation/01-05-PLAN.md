---
phase: 01-multi-tenant-security-foundation
plan: 05
type: tdd
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - platform/backend/src/__tests__/unit/credentials/CredentialProvider.test.ts
  - platform/backend/src/__tests__/unit/credentials/EnvironmentProvider.test.ts
  - platform/backend/src/__tests__/unit/credentials/FileProvider.test.ts
  - platform/backend/src/__tests__/unit/credentials/AwsSsmProvider.test.ts
  - platform/backend/src/__tests__/unit/credentials/CredentialProviderFactory.test.ts
  - platform/backend/src/__tests__/unit/api/middleware/tenantValidation.test.ts
  - platform/backend/src/__tests__/integration/credentials.integration.test.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All providers have unit tests covering get/put/delete/isAvailable operations"
    - "Factory tests verify fallback chain behavior and logging"
    - "Middleware tests validate tenant extraction and enforcement"
    - "Integration tests verify end-to-end credential operations"
    - "No credential values appear in any test output"
  artifacts:
    - path: "platform/backend/src/__tests__/unit/credentials/"
      provides: "Unit tests for credential system"
      contains: ["CredentialProvider.test.ts", "EnvironmentProvider.test.ts", "FileProvider.test.ts", "AwsSsmProvider.test.ts", "CredentialProviderFactory.test.ts"]
    - path: "platform/backend/src/__tests__/unit/api/middleware/tenantValidation.test.ts"
      provides: "Unit tests for tenant validation"
      contains: "Tests for tenantMiddleware, validateTenantAccess, credentialAccessMiddleware"
    - path: "platform/backend/src/__tests__/integration/credentials.integration.test.ts"
      provides: "Integration tests for credential system"
      contains: "End-to-end tests with factory and all providers"
  key_links:
    - from: "Test files"
      to: "Source files"
      via: "import statements and test coverage"
      pattern: "import.*from.*\.\.\/\.\.\/credentials"
---

<objective>
Write comprehensive tests for the credential system using TDD methodology. This plan ensures all components work correctly together and that the security guarantees (no credential leakage, tenant isolation) are verified.

Purpose: Validate the entire credential system through unit tests (each provider in isolation), factory tests (fallback chain behavior), middleware tests (tenant validation), and integration tests (end-to-end flows).

Output: Complete test suite covering all credential providers, factory, middleware, and integration scenarios with proper mocking and security validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-tenant-security-foundation/01-CONTEXT.md
@.planning/phases/01-multi-tenant-security-foundation/01-RESEARCH.md

@.planning/phases/01-multi-tenant-security-foundation/01-01-SUMMARY.md
@.planning/phases/01-multi-tenant-security-foundation/01-02-SUMMARY.md
@.planning/phases/01-multi-tenant-security-foundation/01-03-SUMMARY.md
@.planning/phases/01-multi-tenant-security-foundation/01-04-SUMMARY.md
</context>

<feature>
  <name>Environment Provider Tests</name>
  <files>platform/backend/src/__tests__/unit/credentials/EnvironmentProvider.test.ts</files>
  <behavior>
    Given: An EnvironmentProvider instance
    When: Getting a credential that exists in process.env
    Then: Returns the credential value

    Given: An EnvironmentProvider instance
    When: Getting a credential that does not exist
    Then: Returns null

    Given: An EnvironmentProvider instance
    When: Calling put()
    Then: Throws "read-only" error

    Given: An EnvironmentProvider instance
    When: Calling delete()
    Then: Throws "read-only" error

    Given: An EnvironmentProvider instance
    When: Calling isAvailable()
    Then: Returns true

    Given: An EnvironmentProvider instance
    When: Getting "github_token"
    Then: Reads from GITHUB_TOKEN environment variable

    Given: An EnvironmentProvider instance
    When: Listing keys
    Then: Returns environment variables containing TOKEN, API_KEY, PASSWORD, SECRET, CREDENTIAL
  </behavior>
  <implementation>
Create test file with Jest, mock process.env, verify key transformation and read-only behavior.
  </implementation>
</feature>

<feature>
  <name>File Provider Tests</name>
  <files>platform/backend/src/__tests__/unit/credentials/FileProvider.test.ts</files>
  <behavior>
    Given: A FileProvider with valid encryption key
    When: Writing a credential
    Then: Credential is encrypted and written to file

    Given: A FileProvider with stored credential
    When: Reading the credential
    Then: Returns the decrypted value

    Given: A FileProvider with stored credential
    When: Deleting the credential
    Then: Credential is removed from file

    Given: A FileProvider with multiple tenants
    When: Reading tenant-specific credential
    Then: Only returns that tenant's credentials

    Given: A FileProvider without encryption key
    When: Instantiating
    Then: Throws descriptive error

    Given: A FileProvider with invalid file
    When: Reading
    Then: Returns empty store (creates new)

    Given: A FileProvider after encrypting data
    When: Creating new instance with same key
    Then: Can decrypt previously stored data
  </behavior>
  <implementation>
Create test file with Jest, use temporary files, verify encryption/decryption round-trip, tenant isolation.
  </implementation>
</feature>

<feature>
  <name>AWS SSM Provider Tests</name>
  <files>platform/backend/src/__tests__/unit/credentials/AwsSsmProvider.test.ts</files>
  <behavior>
    Given: An AwsSsmProvider with mocked SSM client
    When: Getting an existing parameter
    Then: Returns the parameter value

    Given: An AwsSsmProvider with mocked SSM client
    When: Getting a non-existent parameter
    Then: Returns null

    Given: An AwsSsmProvider with mocked SSM client
    When: Putting a credential
    Then: Calls PutParameterCommand with SecureString

    Given: An AwsSsmProvider with mocked SSM client
    When: Deleting a credential
    Then: Calls DeleteParameterCommand

    Given: An AwsSsmProvider with mocked SSM client
    When: Building path for tenant "tenant-123" and key "GITHUB_TOKEN"
    Then: Returns "/viberator/tenants/tenant-123/GITHUB_TOKEN"

    Given: An AwsSsmProvider with mocked SSM client
    When: Building path with special characters in tenantId
    Then: Sanitizes to alphanumeric, hyphen, underscore

    Given: An AwsSsmProvider with mocked SSM client
    When: Getting same credential twice
    Then: Uses cached value (second call doesn't hit SSM)

    Given: An AwsSsmProvider with mocked SSM client
    When: SSM throws ParameterNotFound
    Then: Returns null (not an error)
  </behavior>
  <implementation>
Create test file with Jest, mock @aws-sdk/client-ssm commands, verify path building, caching, error handling.
  </implementation>
</feature>

<feature>
  <name>Credential Provider Factory Tests</name>
  <files>platform/backend/src/__tests__/unit/credentials/CredentialProviderFactory.test.ts</files>
  <behavior>
    Given: A factory with Environment and File providers
    When: Getting a credential
    Then: Tries Environment first, then File

    Given: A factory where Environment has the credential
    When: Getting a credential
    Then: Returns Environment value (doesn't try File)

    Given: A factory where Environment fails
    When: Getting a credential
    Then: Tries next provider

    Given: A factory with all providers failing
    When: Getting a credential
    Then: Returns null

    Given: A factory
    When: Putting a credential
    Then: Uses first writable provider (skips Environment)

    Given: A factory
    When: Listing keys
    Then: Uses first provider with listKeys method

    Given: A factory configuration
    When: No providers can initialize
    Then: Throws "No credential providers configured"

    Given: A factory during credential lookup
    When: Provider returns value
    Then: Logs which provider served the request
  </behavior>
  <implementation>
Create test file with Jest, mock providers, verify fallback order, logging, and error handling.
  </implementation>
</feature>

<feature>
  <name>Tenant Validation Middleware Tests</name>
  <files>platform/backend/src/__tests__/unit/api/middleware/tenantValidation.test.ts</files>
  <behavior>
    Given: A request with X-Tenant-Id header
    When: Calling tenantMiddleware
    Then: Adds tenantId to request and calls next()

    Given: A request without tenant identification
    When: Calling tenantMiddleware
    Then: Returns 401 with error message

    Given: A request with invalid tenant ID format
    When: Calling tenantMiddleware
    Then: Returns 400 with validation error

    Given: A request with tenantId
    When: Calling validateTenantAccess with accessible key
    Then: Returns true

    Given: A request with tenantId
    When: Calling credentialAccessMiddleware
    Then: Validates tenant can access the credential

    Given: A request with tenantId in query
    When: Calling tenantMiddleware
    Then: Extracts tenantId from query parameter

    Given: A request without tenantId
    When: Calling credentialAccessMiddleware
    Then: Returns 401

    Given: A request for admin-only route
    When: Calling requireTenantTenant with different tenant
    Then: Returns 403
  </behavior>
  <implementation>
Create test file with Jest, mock Express request/response, verify tenant extraction, validation, error responses.
  </implementation>
</feature>

<feature>
  <name>Credential Integration Tests</name>
  <files>platform/backend/src/__tests__/integration/credentials.integration.test.ts</files>
  <behavior>
    Given: A configured factory with File provider
    When: Storing and retrieving a credential
    Then: Returns the same value

    Given: A configured factory with Environment provider
    When: Setting process.env and retrieving
    Then: Returns the environment variable value

    Given: A configured factory
    When: Getting credential for different tenants
    Then: Each tenant gets only their own credentials

    Given: A configured factory
    When: Putting and deleting credential
    Then: Credential is removed after delete

    Given: A configured factory
    When: Provider fails during get operation
    Then: Falls back to next provider with warning log

    Given: A factory with redaction enabled
    When: Logging credential operations
    Then: No actual values appear in logs

    Given: The credential system
    When: All providers are unavailable
    Then: Operations fail gracefully with null returns
  </behavior>
  <implementation>
Create integration test file, use real File provider with temp files, mock AWS provider, verify end-to-end flows.
  </implementation>
</feature>

<tasks>

**RED Phase: Write failing tests**

For each feature above:
1. Create the test file
2. Write tests describing expected behavior
3. Run tests - verify they fail (RED)
4. Commit: `test(01-01): add failing tests for [feature]`

**GREEN Phase: Make tests pass**

For tests that fail due to missing implementation:
1. Implement the minimal code to make test pass
2. Run tests - verify they pass (GREEN)
3. Commit: `feat(01-01): implement [feature]`

For our case, implementation is already complete from plans 01-01 through 01-04, so:
1. Run tests - verify they pass
2. If any tests fail, fix implementation
3. Commit: `test(01-05): add credential system tests`

**REFACTOR Phase: Clean up (if needed)**

If obvious improvements exist after tests pass:
1. Refactor code
2. Run tests - verify still pass
3. Commit only if changes made

</tasks>

<verification>
1. All unit tests pass: `cd platform/backend && npm test -- testPathPattern=unit/credentials`
2. All middleware tests pass: `cd platform/backend && npm test -- testPathPattern=unit/api/middleware`
3. Integration tests pass: `cd platform/backend && npm test -- testPathPattern=integration/credentials`
4. Test coverage is above 80% for credential system
5. No credential values appear in test output
</verification>

<success_criteria>
- Unit tests exist for all providers
- Unit tests exist for factory
- Unit tests exist for middleware
- Integration tests cover end-to-end flows
- All tests pass
- Coverage threshold met
- No credential leakage in test output
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-security-foundation/01-05-SUMMARY.md` with:
- Test coverage summary
- Any discovered issues or edge cases
- Recommendations for future testing
</output>
